import{_ as r,C as n,o as d,c as l,j as o,a as t,E as i,w as a,ao as c}from"./chunks/framework.BduQNWk1.js";const J=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"TimeseriesMakie/reference.md","filePath":"TimeseriesMakie/reference.md","lastUpdated":null}'),p={name:"TimeseriesMakie/reference.md"},h={class:"jldocstring custom-block",open:""},u={class:"jldocstring custom-block",open:""},g={class:"jldocstring custom-block",open:""},m={class:"jldocstring custom-block",open:""},f={class:"jldocstring custom-block",open:""},b={class:"jldocstring custom-block",open:""},k={class:"jldocstring custom-block",open:""},y={class:"jldocstring custom-block",open:""},w={class:"jldocstring custom-block",open:""},T={class:"jldocstring custom-block",open:""},v={class:"jldocstring custom-block",open:""},j={class:"jldocstring custom-block",open:""},M={class:"jldocstring custom-block",open:""},x={class:"jldocstring custom-block",open:""},_={class:"jldocstring custom-block",open:""},S={class:"jldocstring custom-block",open:""},C={class:"jldocstring custom-block",open:""},A={class:"jldocstring custom-block",open:""},E={class:"jldocstring custom-block",open:""},I={class:"jldocstring custom-block",open:""},F={class:"jldocstring custom-block",open:""},P={class:"jldocstring custom-block",open:""},L={class:"jldocstring custom-block",open:""},O={class:"jldocstring custom-block",open:""},D={class:"jldocstring custom-block",open:""},z={class:"jldocstring custom-block",open:""},N={class:"jldocstring custom-block",open:""},q={class:"jldocstring custom-block",open:""},V={class:"jldocstring custom-block",open:""},B={class:"jldocstring custom-block",open:""},R={class:"jldocstring custom-block",open:""},G={class:"jldocstring custom-block",open:""},Y={class:"jldocstring custom-block",open:""};function U(W,e,K,H,$,X){const s=n("Badge");return d(),l("div",null,[o("details",h,[o("summary",null,[e[0]||(e[0]=o("a",{id:"TimeseriesMakie.Kinetic",href:"#TimeseriesMakie.Kinetic"},[o("span",{class:"jlbinding"},"TimeseriesMakie.Kinetic")],-1)),e[1]||(e[1]=t()),i(s,{type:"info",class:"jlObjectType jlType",text:"Type"})]),e[3]||(e[3]=o("p",null,[o("code",null,"Kinetic"),t(" is the plot type associated with plotting function "),o("code",null,"kinetic"),t(". Check the docstring for "),o("code",null,"kinetic"),t(" for further information.")],-1)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[2]||(e[2]=[o("a",{href:"https://github.com/MakieOrg/Makie.jl/blob/master/src/recipes.jl#L557",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",u,[o("summary",null,[e[4]||(e[4]=o("a",{id:"TimeseriesMakie.PSTH",href:"#TimeseriesMakie.PSTH"},[o("span",{class:"jlbinding"},"TimeseriesMakie.PSTH")],-1)),e[5]||(e[5]=t()),i(s,{type:"info",class:"jlObjectType jlType",text:"Type"})]),e[7]||(e[7]=o("p",null,[o("code",null,"PSTH"),t(" is the plot type associated with plotting function "),o("code",null,"psth"),t(". Check the docstring for "),o("code",null,"psth"),t(" for further information.")],-1)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[6]||(e[6]=[o("a",{href:"https://github.com/MakieOrg/Makie.jl/blob/master/src/recipes.jl#L557",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",g,[o("summary",null,[e[8]||(e[8]=o("a",{id:"TimeseriesMakie.RateMap",href:"#TimeseriesMakie.RateMap"},[o("span",{class:"jlbinding"},"TimeseriesMakie.RateMap")],-1)),e[9]||(e[9]=t()),i(s,{type:"info",class:"jlObjectType jlType",text:"Type"})]),e[11]||(e[11]=o("p",null,[o("code",null,"RateMap"),t(" is the plot type associated with plotting function "),o("code",null,"ratemap"),t(". Check the docstring for "),o("code",null,"ratemap"),t(" for further information.")],-1)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[10]||(e[10]=[o("a",{href:"https://github.com/MakieOrg/Makie.jl/blob/master/src/recipes.jl#L557",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",m,[o("summary",null,[e[12]||(e[12]=o("a",{id:"TimeseriesMakie.Shadows",href:"#TimeseriesMakie.Shadows"},[o("span",{class:"jlbinding"},"TimeseriesMakie.Shadows")],-1)),e[13]||(e[13]=t()),i(s,{type:"info",class:"jlObjectType jlType",text:"Type"})]),e[15]||(e[15]=o("p",null,[o("code",null,"Shadows"),t(" is the plot type associated with plotting function "),o("code",null,"shadows"),t(". Check the docstring for "),o("code",null,"shadows"),t(" for further information.")],-1)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[14]||(e[14]=[o("a",{href:"https://github.com/MakieOrg/Makie.jl/blob/master/src/recipes.jl#L557",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",f,[o("summary",null,[e[16]||(e[16]=o("a",{id:"TimeseriesMakie.SpikeRaster",href:"#TimeseriesMakie.SpikeRaster"},[o("span",{class:"jlbinding"},"TimeseriesMakie.SpikeRaster")],-1)),e[17]||(e[17]=t()),i(s,{type:"info",class:"jlObjectType jlType",text:"Type"})]),e[19]||(e[19]=o("p",null,[o("code",null,"SpikeRaster"),t(" is the plot type associated with plotting function "),o("code",null,"spikeraster"),t(". Check the docstring for "),o("code",null,"spikeraster"),t(" for further information.")],-1)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[18]||(e[18]=[o("a",{href:"https://github.com/MakieOrg/Makie.jl/blob/master/src/recipes.jl#L557",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",b,[o("summary",null,[e[20]||(e[20]=o("a",{id:"TimeseriesMakie.Traces",href:"#TimeseriesMakie.Traces"},[o("span",{class:"jlbinding"},"TimeseriesMakie.Traces")],-1)),e[21]||(e[21]=t()),i(s,{type:"info",class:"jlObjectType jlType",text:"Type"})]),e[23]||(e[23]=o("p",null,[o("code",null,"Traces"),t(" is the plot type associated with plotting function "),o("code",null,"traces"),t(". Check the docstring for "),o("code",null,"traces"),t(" for further information.")],-1)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[22]||(e[22]=[o("a",{href:"https://github.com/MakieOrg/Makie.jl/blob/master/src/recipes.jl#L557",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",k,[o("summary",null,[e[24]||(e[24]=o("a",{id:"TimeseriesMakie.Trail",href:"#TimeseriesMakie.Trail"},[o("span",{class:"jlbinding"},"TimeseriesMakie.Trail")],-1)),e[25]||(e[25]=t()),i(s,{type:"info",class:"jlObjectType jlType",text:"Type"})]),e[27]||(e[27]=o("p",null,[o("code",null,"Trail"),t(" is the plot type associated with plotting function "),o("code",null,"trail"),t(". Check the docstring for "),o("code",null,"trail"),t(" for further information.")],-1)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[26]||(e[26]=[o("a",{href:"https://github.com/MakieOrg/Makie.jl/blob/master/src/recipes.jl#L557",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",y,[o("summary",null,[e[28]||(e[28]=o("a",{id:"TimeseriesMakie.Trajectory",href:"#TimeseriesMakie.Trajectory"},[o("span",{class:"jlbinding"},"TimeseriesMakie.Trajectory")],-1)),e[29]||(e[29]=t()),i(s,{type:"info",class:"jlObjectType jlType",text:"Type"})]),e[31]||(e[31]=o("p",null,[o("code",null,"Trajectory"),t(" is the plot type associated with plotting function "),o("code",null,"trajectory"),t(". Check the docstring for "),o("code",null,"trajectory"),t(" for further information.")],-1)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[30]||(e[30]=[o("a",{href:"https://github.com/MakieOrg/Makie.jl/blob/master/src/recipes.jl#L557",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",w,[o("summary",null,[e[32]||(e[32]=o("a",{id:"TimeseriesMakie.ballradius-Tuple{Any, Any, Any}",href:"#TimeseriesMakie.ballradius-Tuple{Any, Any, Any}"},[o("span",{class:"jlbinding"},"TimeseriesMakie.ballradius")],-1)),e[33]||(e[33]=t()),i(s,{type:"info",class:"jlObjectType jlMethod",text:"Method"})]),e[35]||(e[35]=c('<div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light-ianthina-comments github-dark-ianthina-comments vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ballradius</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(X, Y, cap)</span></span></code></pre></div><p>Radius of the largest disc tangent to the curve at each point that contains no other part of it, in the pixel coordinates <code>X</code>, <code>Y</code>, saturating at <code>cap</code>.</p><p>For a circle of radius <code>R</code> this returns <code>R</code>; for two strands <code>d</code> apart it returns <code>d/2</code>. A tight turn and a near miss are therefore one measurement, of how much room the curve has, and the stroke has only to fit inside it. The disc tangent at <code>Pᵢ</code> on the normal side, centred <code>Pᵢ + r nᵢ</code>, contains <code>Pⱼ</code> exactly when <code>|d|² &lt; 2r (d·nᵢ)</code> with <code>d = Pⱼ - Pᵢ</code>.</p><p>No arc-length exclusion is needed: an arc-neighbour of a smooth curve gives <code>|d|²/(2 d·nᵢ) →</code> the osculating radius by itself, so curvature is the limit of the same formula rather than a separate case. Only points within <code>2cap</code> can bind the result, so they are bucketed into cells of that size and only the neighbouring nine are scanned, giving expected <code>O(n)</code>.</p>',4)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[34]||(e[34]=[o("a",{href:"https://github.com/brendanjohnharris/TimeseriesMakie.jl/blob/main/src/recipes/kinetic.jl#L92-L107",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",T,[o("summary",null,[e[36]||(e[36]=o("a",{id:"TimeseriesMakie.crowdwidths-Tuple{AbstractVector{<:Point2}, Any, Any, Any}",href:"#TimeseriesMakie.crowdwidths-Tuple{AbstractVector{<:Point2}, Any, Any, Any}"},[o("span",{class:"jlbinding"},"TimeseriesMakie.crowdwidths")],-1)),e[37]||(e[37]=t()),i(s,{type:"info",class:"jlObjectType jlMethod",text:"Method"})]),e[39]||(e[39]=c('<div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light-ianthina-comments github-dark-ianthina-comments vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">crowdwidths</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(P, widthrange, crowding, taper)</span></span></code></pre></div><p>One width per point of the pixel-space path <code>P</code>, so that the stroke fits the room the curve has.</p><p><code>ballradius</code> gives that room; <code>crowding</code> is the fraction of it the stroke fills (1 means two neighbouring strands just touch, leaving no visible gap). The bound is then eroded over a disc, and finally limited to a relative rate of change of <code>taper</code> per pixel travelled along the curve: a <em>relative</em> limit, because a change from 1 to 2 px reads as strongly as one from 5 to 10 px.</p>',3)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[38]||(e[38]=[o("a",{href:"https://github.com/brendanjohnharris/TimeseriesMakie.jl/blob/main/src/recipes/kinetic.jl#L216-L225",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",v,[o("summary",null,[e[40]||(e[40]=o("a",{id:"TimeseriesMakie.kinetic",href:"#TimeseriesMakie.kinetic"},[o("span",{class:"jlbinding"},"TimeseriesMakie.kinetic")],-1)),e[41]||(e[41]=t()),i(s,{type:"info",class:"jlObjectType jlFunction",text:"Function"})]),e[43]||(e[43]=c('<div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light-ianthina-comments github-dark-ianthina-comments vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">kinetic</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x, y; kwargs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Plots a line with a varying width.</p><p><strong>Key attributes</strong></p><p><code>linewidth</code> = <code>:curv</code>: the width profile along the line, one value per point.</p><ul><li><p><code>:curv</code> - as wide as the curve has room for. Crowded stretches draw narrow, so a fast passage stays legible beside a slow one; open stretches draw at the full width</p></li><li><p><code>:x</code>, <code>:y</code> - from that coordinate, mapped onto <code>widthrange</code></p></li><li><p><code>&lt;: Number</code> - constant</p></li></ul><p><code>widthrange</code> = <code>(1, 11)</code>: the narrowest and widest the stroke may draw, in pixels.</p><p><code>linewidthscale</code> = <code>1</code>: Scale factor for the line width.</p><p><strong>Crowding</strong></p><p><code>:curv</code> measures, at each point, the radius of the largest disc tangent to the curve holding no other part of it: for a circle of radius <code>R</code> that is <code>R</code>, and for two strands <code>d</code> apart it is <code>d/2</code>. A tight turn and a near miss are one quantity, of how much room there is, and the stroke has only to fit inside it. Because the room is measured in pixels, the widths respond to zoom: magnify a dense passage and the stroke fattens as its structure becomes resolvable.</p><p><code>crowding</code> = <code>0.5</code>: the fraction of that room the stroke fills. <code>1</code> lets two neighbouring strands just touch, leaving no visible gap.</p><p><code>taper</code> = <code>0.02</code>: the largest relative change in width per pixel travelled along the curve. It also sets how far a narrow spot spreads into its surroundings, and the two cannot be separated: lower it to flatten the width across an oscillation, raise it to keep isolated thin loops at their full width.</p><p><code>geometry</code> = <code>:auto</code>: how the stroke is drawn. Read once at construction; changing it later has no effect.</p><ul><li><p><code>:auto</code> - <code>:lines</code> on GLMakie and WGLMakie, <code>:mesh</code> on every other backend</p></li><li><p><code>:mesh</code> - a stroke built in pixel space. Continuous taper, uniform transparency and a width isotropic in pixels on any axis, on every backend; caps and joins are always round. A uniform colour draws as a single filled outline, which CairoMakie anti-aliases; a per-point colour needs triangles, which it paints through a Cairo mesh pattern and leaves with hard edges</p></li><li><p><code>:lines</code> - a single <code>lines</code> carrying one width per point. Renders as <code>:mesh</code> does on GLMakie and WGLMakie, which stroke a varying width natively; CairoMakie cannot, and errors</p></li><li><p><code>:segments</code> - a <code>linesegments</code> with one width per segment, hiding the joints behind <code>linecap</code>. Works everywhere, but the width steps between segments and translucent colours bead at the joints</p></li></ul><p><em>Other attributes are shared with <code>Makie.Lines</code>.</em></p><p><strong>Plot type</strong></p><p>The plot type alias for the <code>kinetic</code> function is <code>Kinetic</code>.</p><p><strong>Attributes</strong></p><p><strong><code>alpha</code></strong> = <code>1.0</code> — The alpha value of the colormap or color attribute. Multiple alphas like in <code>plot(alpha=0.2, color=(:red, 0.5))</code>, will get multiplied.</p><p><strong><code>clip_planes</code></strong> = <code>@inherit clip_planes automatic</code> — Clip planes offer a way to do clipping in 3D space. You can set a Vector of up to 8 <code>Plane3f</code> planes here, behind which plots will be clipped (i.e. become invisible). By default clip planes are inherited from the parent plot or scene. You can remove parent <code>clip_planes</code> by passing <code>Plane3f[]</code>.</p><p><strong><code>color</code></strong> = <code>@inherit linecolor</code> — <em>No docs available.</em></p><p><strong><code>colormap</code></strong> = <code>@inherit colormap :viridis</code> — Sets the colormap that is sampled for numeric <code>color</code>s. <code>PlotUtils.cgrad(...)</code>, <code>Makie.Reverse(any_colormap)</code> can be used as well, or any symbol from ColorBrewer or PlotUtils. To see all available color gradients, you can call <code>Makie.available_gradients()</code>.</p><p><strong><code>colorrange</code></strong> = <code>automatic</code> — The values representing the start and end points of <code>colormap</code>.</p><p><strong><code>colorscale</code></strong> = <code>identity</code> — The color transform function. Can be any function, but only works well together with <code>Colorbar</code> for <code>identity</code>, <code>log</code>, <code>log2</code>, <code>log10</code>, <code>sqrt</code>, <code>logit</code>, <code>Makie.pseudolog10</code>, <code>Makie.Symlog10</code>, <code>Makie.AsinhScale</code>, <code>Makie.SinhScale</code>, <code>Makie.LogScale</code>, <code>Makie.LuptonAsinhScale</code>, and <code>Makie.PowerScale</code>.</p><p><strong><code>crowding</code></strong> = <code>0.5</code> — Fraction of the available room a <code>:curv</code> stroke fills. <code>1</code> lets two neighbouring strands just touch, leaving no visible gap between them.</p><p><strong><code>cycle</code></strong> = <code>:color</code> — <em>No docs available.</em></p><p><strong><code>depth_shift</code></strong> = <code>0.0</code> — Adjusts the depth value of a plot after all other transformations, i.e. in clip space, where <code>-1 &lt;= depth &lt;= 1</code>. This only applies to GLMakie and WGLMakie and can be used to adjust render order (like a tunable overdraw).</p><p><strong><code>fxaa</code></strong> = <code>false</code> — Adjusts whether the plot is rendered with fxaa (fast approximate anti-aliasing, GLMakie only). Note that some plots implement a better native anti-aliasing solution (scatter, text, lines). For them <code>fxaa = true</code> generally lowers quality. Plots that show smoothly interpolated data (e.g. image, surface) may also degrade in quality as <code>fxaa = true</code> can cause blurring.</p><p><strong><code>geometry</code></strong> = <code>:auto</code> — How the stroke is drawn; see the docstring. Read once at construction.</p><p><strong><code>highclip</code></strong> = <code>automatic</code> — The color for any value above the colorrange.</p><p><strong><code>inspectable</code></strong> = <code>@inherit inspectable</code> — Sets whether this plot should be seen by <code>DataInspector</code>. The default depends on the theme of the parent scene.</p><p><strong><code>inspector_clear</code></strong> = <code>automatic</code> — Sets a callback function <code>(inspector, plot) -&gt; ...</code> for cleaning up custom indicators in DataInspector.</p><p><strong><code>inspector_hover</code></strong> = <code>automatic</code> — Sets a callback function <code>(inspector, plot, index) -&gt; ...</code> which replaces the default <code>show_data</code> methods.</p><p><strong><code>inspector_label</code></strong> = <code>automatic</code> — Sets a callback function <code>(plot, index, position) -&gt; string</code> which replaces the default label generated by DataInspector.</p><p><strong><code>joinstyle</code></strong> = <code>@inherit joinstyle</code> — Controls the rendering at corners. Options are <code>:miter</code> for sharp corners, <code>:bevel</code> for &quot;cut off&quot; corners, and <code>:round</code> for rounded corners. If the corner angle is below <code>miter_limit</code>, <code>:miter</code> is equivalent to <code>:bevel</code> to avoid long spikes.</p><p><strong><code>linecap</code></strong> = <code>:round</code> — <em>No docs available.</em></p><p><strong><code>linestyle</code></strong> = <code>nothing</code> — Sets the dash pattern of the line. Options are <code>:solid</code> (equivalent to <code>nothing</code>), <code>:dot</code>, <code>:dash</code>, <code>:dashdot</code> and <code>:dashdotdot</code>. These can also be given in a tuple with a gap style modifier, either <code>:normal</code>, <code>:dense</code> or <code>:loose</code>. For example, <code>(:dot, :loose)</code> or <code>(:dashdot, :dense)</code>.</p><p>For custom patterns have a look at <a href="./@ref"><code>Makie.Linestyle</code></a>.</p><p><strong><code>linewidth</code></strong> = <code>:curv</code> — <em>No docs available.</em></p><p><strong><code>linewidthscale</code></strong> = <code>1</code> — <em>No docs available.</em></p><p><strong><code>lowclip</code></strong> = <code>automatic</code> — The color for any value below the colorrange.</p><p><strong><code>miter_limit</code></strong> = <code>@inherit miter_limit</code> — Sets the minimum inner join angle below which miter joins truncate. See also <code>Makie.miter_distance_to_angle</code>.</p><p><strong><code>model</code></strong> = <code>automatic</code> — Sets a model matrix for the plot. This overrides adjustments made with <code>translate!</code>, <code>rotate!</code> and <code>scale!</code>.</p><p><strong><code>nan_color</code></strong> = <code>:transparent</code> — The color for NaN values.</p><p><strong><code>overdraw</code></strong> = <code>false</code> — Controls if the plot will draw over other plots. This specifically means ignoring depth checks in GL backends</p><p><strong><code>rasterize</code></strong> = <code>@inherit rasterize false</code> — Controls whether CairoMakie rasterizes a plot (true, &gt;0) or not (false, 0). If an integer is given, it sets the pixel density for rasterization.</p><p><strong><code>space</code></strong> = <code>:data</code> — Sets the transformation space for box encompassing the plot. See <code>Makie.spaces()</code> for possible inputs.</p><p><strong><code>ssao</code></strong> = <code>false</code> — Adjusts whether the plot is rendered with ssao (screen space ambient occlusion). Note that this only makes sense in 3D plots and is only applicable with <code>fxaa = true</code>.</p><p><strong><code>taper</code></strong> = <code>0.02</code> — Largest relative change in a <code>:curv</code> width per pixel travelled along the curve.</p><p><strong><code>transformation</code></strong> = <code>:automatic</code> — Controls the inheritance or directly sets the transformations of a plot. Transformations include the transform function and model matrix as generated by <code>translate!(...)</code>, <code>scale!(...)</code> and <code>rotate!(...)</code>. They can be set directly by passing a <code>Transformation()</code> object or inherited from the parent plot or scene. Inheritance options include:</p><ul><li><p><code>:automatic</code>: Inherit transformations if the parent and child <code>space</code> is compatible</p></li><li><p><code>:inherit</code>: Inherit transformations</p></li><li><p><code>:inherit_model</code>: Inherit only model transformations</p></li><li><p><code>:inherit_transform_func</code>: Inherit only the transform function</p></li><li><p><code>:nothing</code>: Inherit neither, fully disconnecting the child&#39;s transformations from the parent</p></li></ul><p>Another option is to pass arguments to the <code>transform!()</code> function which then get applied to the plot. For example <code>transformation = (:xz, 1.0)</code> which rotates the <code>xy</code> plane to the <code>xz</code> plane and translates by <code>1.0</code>. For this inheritance defaults to <code>:automatic</code> but can also be set through e.g. <code>(:nothing, (:xz, 1.0))</code>.</p><p><strong><code>transparency</code></strong> = <code>false</code> — Adjusts how the plot deals with transparency. In GLMakie <code>transparency = true</code> results in using Order Independent Transparency.</p><p><strong><code>visible</code></strong> = <code>true</code> — Controls whether the plot gets rendered or not.</p><p><strong><code>widthrange</code></strong> = <code>(1, 11)</code> — The narrowest and widest the stroke may draw, in pixels.</p>',54)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[42]||(e[42]=[o("a",{href:"https://github.com/MakieOrg/Makie.jl/blob/master/src/recipes.jl#L556-L699",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",j,[o("summary",null,[e[44]||(e[44]=o("a",{id:"TimeseriesMakie.kinetic!",href:"#TimeseriesMakie.kinetic!"},[o("span",{class:"jlbinding"},"TimeseriesMakie.kinetic!")],-1)),e[45]||(e[45]=t()),i(s,{type:"info",class:"jlObjectType jlFunction",text:"Function"})]),e[47]||(e[47]=o("p",null,[o("code",null,"kinetic!"),t(" is the mutating variant of plotting function "),o("code",null,"kinetic"),t(". Check the docstring for "),o("code",null,"kinetic"),t(" for further information.")],-1)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[46]||(e[46]=[o("a",{href:"https://github.com/MakieOrg/Makie.jl/blob/master/src/recipes.jl#L558",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",M,[o("summary",null,[e[48]||(e[48]=o("a",{id:"TimeseriesMakie.minmax-Tuple{Any}",href:"#TimeseriesMakie.minmax-Tuple{Any}"},[o("span",{class:"jlbinding"},"TimeseriesMakie.minmax")],-1)),e[49]||(e[49]=t()),i(s,{type:"info",class:"jlObjectType jlMethod",text:"Method"})]),e[51]||(e[51]=c('<div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light-ianthina-comments github-dark-ianthina-comments vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">minmax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x)</span></span></code></pre></div><p>Normalise <code>x</code> onto <code>[0, 1]</code>. A constant <code>x</code> has no ordering to normalise, so it maps to the middle of the range rather than to <code>0/0</code>; callers use the result to index colormaps and alpha profiles, where a <code>NaN</code> silently renders nothing.</p>',2)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[50]||(e[50]=[o("a",{href:"https://github.com/brendanjohnharris/TimeseriesMakie.jl/blob/main/src/TimeseriesMakie.jl#L37-L43",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",x,[o("summary",null,[e[52]||(e[52]=o("a",{id:"TimeseriesMakie.plotspectrum",href:"#TimeseriesMakie.plotspectrum"},[o("span",{class:"jlbinding"},"TimeseriesMakie.plotspectrum")],-1)),e[53]||(e[53]=t()),i(s,{type:"info",class:"jlObjectType jlFunction",text:"Function"})]),e[55]||(e[55]=c('<div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light-ianthina-comments github-dark-ianthina-comments vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">plotspectrum</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x; axis </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (), kwargs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Plot a spectrum as <a href="/TimeseriesDocs.jl/dev/TimeseriesMakie/reference#TimeseriesMakie.spectrumplot"><code>spectrumplot</code></a> does, into an axis that is additionally given log scales, limits covering the positive data, and axis labels carrying the units of <code>x</code>. Requires the <code>TimeseriesTools</code> extension (<code>using TimeseriesTools</code>).</p>',2)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[54]||(e[54]=[o("a",{href:"https://github.com/brendanjohnharris/TimeseriesMakie.jl/blob/main/src/TimeseriesMakie.jl#L104-L110",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",_,[o("summary",null,[e[56]||(e[56]=o("a",{id:"TimeseriesMakie.pop_rasterize!-Tuple{Any}",href:"#TimeseriesMakie.pop_rasterize!-Tuple{Any}"},[o("span",{class:"jlbinding"},"TimeseriesMakie.pop_rasterize!")],-1)),e[57]||(e[57]=t()),i(s,{type:"info",class:"jlObjectType jlMethod",text:"Method"})]),e[59]||(e[59]=c('<div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light-ianthina-comments github-dark-ianthina-comments vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">pop_rasterize!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(plot)</span></span></code></pre></div><p>Remove <code>rasterize</code> from a recipe&#39;s <code>plot.kw</code> and return it, for forwarding to the child plot.</p><p><code>rasterize</code> reaches a plot through <code>plot.kw</code> and is registered on it only after <code>plot!</code> returns. Left on a recipe, CairoMakie treats that recipe as atomic and hands it to an unsupported <code>draw_atomic</code>: a warning, and no rasterisation. Only worth doing when the child is a primitive; giving it to another recipe just moves the problem down a level.</p>',3)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[58]||(e[58]=[o("a",{href:"https://github.com/brendanjohnharris/TimeseriesMakie.jl/blob/main/src/TimeseriesMakie.jl#L52-L61",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",S,[o("summary",null,[e[60]||(e[60]=o("a",{id:"TimeseriesMakie.psth",href:"#TimeseriesMakie.psth"},[o("span",{class:"jlbinding"},"TimeseriesMakie.psth")],-1)),e[61]||(e[61]=t()),i(s,{type:"info",class:"jlObjectType jlFunction",text:"Function"})]),e[63]||(e[63]=c('<div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light-ianthina-comments github-dark-ianthina-comments vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">psth</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(times; kwargs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Peri-stimulus time histogram: bin spike <code>times</code> into a histogram of population activity over time.</p><p><strong>Key attributes:</strong></p><ul><li><p><code>binwidth</code> = <code>automatic</code>: bin width in time units (<code>automatic</code> uses the time range / 50).</p></li><li><p><code>normalization</code> = <code>:rate</code>: <code>:count</code> (spikes per bin) or <code>:rate</code> (spikes per unit time). With <code>nneurons</code> set, the rate is additionally divided by the neuron count (a per-neuron rate).</p></li><li><p><code>nneurons</code> = <code>automatic</code>: divide the rate by this many neurons (per-neuron normalisation).</p></li></ul><p><em>Other attributes are shared with <code>Makie.BarPlot</code>.</em></p><p><strong>Plot type</strong></p><p>The plot type alias for the <code>psth</code> function is <code>PSTH</code>.</p><p><strong>Attributes</strong></p><p><strong><code>alpha</code></strong> = <code>1.0</code> — The alpha value of the colormap or color attribute. Multiple alphas like in <code>plot(alpha=0.2, color=(:red, 0.5))</code>, will get multiplied.</p><p><strong><code>bar_labels</code></strong> = <code>nothing</code> — Labels added at the end of each bar.</p><p><strong><code>binwidth</code></strong> = <code>automatic</code> — Bin width in time units (<code>automatic</code> = time range / 50).</p><p><strong><code>clip_planes</code></strong> = <code>@inherit clip_planes automatic</code> — Clip planes offer a way to do clipping in 3D space. You can set a Vector of up to 8 <code>Plane3f</code> planes here, behind which plots will be clipped (i.e. become invisible). By default clip planes are inherited from the parent plot or scene. You can remove parent <code>clip_planes</code> by passing <code>Plane3f[]</code>.</p><p><strong><code>color</code></strong> = <code>@inherit patchcolor</code> — Sets the color of bars.</p><p><strong><code>color_over_background</code></strong> = <code>automatic</code> — Sets the color of labels that are drawn outside of bars. Defaults to <code>label_color</code></p><p><strong><code>color_over_bar</code></strong> = <code>automatic</code> — Sets the color of labels that are drawn inside of/over bars. Defaults to <code>label_color</code></p><p><strong><code>colormap</code></strong> = <code>@inherit colormap :viridis</code> — Sets the colormap that is sampled for numeric <code>color</code>s. <code>PlotUtils.cgrad(...)</code>, <code>Makie.Reverse(any_colormap)</code> can be used as well, or any symbol from ColorBrewer or PlotUtils. To see all available color gradients, you can call <code>Makie.available_gradients()</code>.</p><p><strong><code>colorrange</code></strong> = <code>automatic</code> — The values representing the start and end points of <code>colormap</code>.</p><p><strong><code>colorscale</code></strong> = <code>identity</code> — The color transform function. Can be any function, but only works well together with <code>Colorbar</code> for <code>identity</code>, <code>log</code>, <code>log2</code>, <code>log10</code>, <code>sqrt</code>, <code>logit</code>, <code>Makie.pseudolog10</code>, <code>Makie.Symlog10</code>, <code>Makie.AsinhScale</code>, <code>Makie.SinhScale</code>, <code>Makie.LogScale</code>, <code>Makie.LuptonAsinhScale</code>, and <code>Makie.PowerScale</code>.</p><p><strong><code>cycle</code></strong> = <code>[:color =&gt; :patchcolor]</code> — Sets which attributes to cycle when creating multiple plots. The values to cycle through are defined by the parent Theme. Multiple cycled attributes can be set by passing a vector. Elements can</p><ul><li><p>directly refer to a cycled attribute, e.g. <code>:color</code></p></li><li><p>map a cycled attribute to a palette attribute, e.g. <code>:linecolor =&gt; :color</code></p></li><li><p>map multiple cycled attributes to a palette attribute, e.g. <code>[:linecolor, :markercolor] =&gt; :color</code></p></li></ul><p><strong><code>depth_shift</code></strong> = <code>0.0</code> — Adjusts the depth value of a plot after all other transformations, i.e. in clip space, where <code>-1 &lt;= depth &lt;= 1</code>. This only applies to GLMakie and WGLMakie and can be used to adjust render order (like a tunable overdraw).</p><p><strong><code>direction</code></strong> = <code>:y</code> — Controls the direction of the bars. can be <code>:y</code> (<code>height</code> is vertical) or <code>:x</code> (<code>height</code> is horizontal).</p><p><strong><code>dodge</code></strong> = <code>automatic</code> — Dodge can be used to separate bars drawn at the same <code>position</code>. For this each bar is given an integer value corresponding to its position relative to the given <code>positions</code>. E.g. with <code>positions = [1, 1, 1, 2, 2, 2]</code> we have 3 bars at each position which can be separated by <code>dodge = [1, 2, 3, 1, 2, 3]</code>.</p><p><strong><code>dodge_gap</code></strong> = <code>0.03</code> — Sets the gap between dodged bars relative to the size of the dodged bars.</p><p><strong><code>fillto</code></strong> = <code>automatic</code> — Controls the baseline of the bars. This is zero in the default <code>automatic</code> case unless the barplot is in a log-scaled <code>Axis</code>. With a log scale, the automatic default is half the minimum value because zero is an invalid value for a log scale.</p><p><strong><code>flip_labels_at</code></strong> = <code>Inf</code> — Sets a <code>height</code> value beyond which labels are drawn inside the bar instead of outside.</p><p><strong><code>fxaa</code></strong> = <code>true</code> — Adjusts whether the plot is rendered with fxaa (fast approximate anti-aliasing, GLMakie only). Note that some plots implement a better native anti-aliasing solution (scatter, text, lines). For them <code>fxaa = true</code> generally lowers quality. Plots that show smoothly interpolated data (e.g. image, surface) may also degrade in quality as <code>fxaa = true</code> can cause blurring.</p><p><strong><code>gap</code></strong> = <code>0</code> — Fraction of each bin left empty between bars.</p><p><strong><code>highclip</code></strong> = <code>automatic</code> — The color for any value above the colorrange.</p><p><strong><code>inspectable</code></strong> = <code>@inherit inspectable</code> — Sets whether this plot should be seen by <code>DataInspector</code>. The default depends on the theme of the parent scene.</p><p><strong><code>inspector_clear</code></strong> = <code>automatic</code> — Sets a callback function <code>(inspector, plot) -&gt; ...</code> for cleaning up custom indicators in DataInspector.</p><p><strong><code>inspector_hover</code></strong> = <code>automatic</code> — Sets a callback function <code>(inspector, plot, index) -&gt; ...</code> which replaces the default <code>show_data</code> methods.</p><p><strong><code>inspector_label</code></strong> = <code>automatic</code> — Sets a callback function <code>(plot, index, position) -&gt; string</code> which replaces the default label generated by DataInspector.</p><p><strong><code>label_align</code></strong> = <code>automatic</code> — Sets the text alignment of labels.</p><p><strong><code>label_color</code></strong> = <code>@inherit textcolor</code> — Sets the color of labels.</p><p><strong><code>label_font</code></strong> = <code>@inherit font</code> — The font of the bar labels.</p><p><strong><code>label_formatter</code></strong> = <code>bar_label_formatter</code> — Formatting function which is applied to bar labels before they are passed on <code>text()</code></p><p><strong><code>label_offset</code></strong> = <code>5</code> — The distance of the labels from the bar ends in screen units. Does not apply when <code>label_position = :center</code>.</p><p><strong><code>label_position</code></strong> = <code>:end</code> — The position of each bar&#39;s label relative to the bar. Possible values are <code>:end</code> or <code>:center</code>.</p><p><strong><code>label_rotation</code></strong> = <code>0π</code> — Sets the text rotation of labels in radians.</p><p><strong><code>label_size</code></strong> = <code>@inherit fontsize</code> — The font size of the bar labels.</p><p><strong><code>lowclip</code></strong> = <code>automatic</code> — The color for any value below the colorrange.</p><p><strong><code>model</code></strong> = <code>automatic</code> — Sets a model matrix for the plot. This overrides adjustments made with <code>translate!</code>, <code>rotate!</code> and <code>scale!</code>.</p><p><strong><code>n_dodge</code></strong> = <code>automatic</code> — Sets the maximum integer for <code>dodge</code>. This sets how many bars can be placed at a given position, controlling their width.</p><p><strong><code>nan_color</code></strong> = <code>:transparent</code> — The color for NaN values.</p><p><strong><code>nneurons</code></strong> = <code>automatic</code> — Divide the rate by this many neurons (<code>automatic</code> = no per-neuron scaling).</p><p><strong><code>normalization</code></strong> = <code>:rate</code> — <code>:count</code> (spikes per bin) or <code>:rate</code> (spikes per unit time).</p><p><strong><code>offset</code></strong> = <code>0.0</code> — Offsets all bars by the given real value. Can also be set per-bar.</p><p><strong><code>overdraw</code></strong> = <code>false</code> — Controls if the plot will draw over other plots. This specifically means ignoring depth checks in GL backends</p><p><strong><code>rasterize</code></strong> = <code>@inherit rasterize false</code> — Controls whether CairoMakie rasterizes a plot (true, &gt;0) or not (false, 0). If an integer is given, it sets the pixel density for rasterization.</p><p><strong><code>space</code></strong> = <code>:data</code> — Sets the transformation space for box encompassing the plot. See <code>Makie.spaces()</code> for possible inputs.</p><p><strong><code>ssao</code></strong> = <code>false</code> — Adjusts whether the plot is rendered with ssao (screen space ambient occlusion). Note that this only makes sense in 3D plots and is only applicable with <code>fxaa = true</code>.</p><p><strong><code>stack</code></strong> = <code>automatic</code> — Similar to <code>dodge</code>, this allows bars at the same <code>positions</code> to be stacked by identifying their stack position with integers. E.g. with <code>positions = [1, 1, 1, 2, 2, 2]</code> each group of 3 bars can be stacked with <code>stack = [1, 2, 3, 1, 2, 3]</code>.</p><p><strong><code>strokecolor</code></strong> = <code>@inherit patchstrokecolor</code> — Sets the outline color of bars.</p><p><strong><code>strokewidth</code></strong> = <code>@inherit patchstrokewidth</code> — Sets the outline linewidth of bars.</p><p><strong><code>transformation</code></strong> = <code>:automatic</code> — Controls the inheritance or directly sets the transformations of a plot. Transformations include the transform function and model matrix as generated by <code>translate!(...)</code>, <code>scale!(...)</code> and <code>rotate!(...)</code>. They can be set directly by passing a <code>Transformation()</code> object or inherited from the parent plot or scene. Inheritance options include:</p><ul><li><p><code>:automatic</code>: Inherit transformations if the parent and child <code>space</code> is compatible</p></li><li><p><code>:inherit</code>: Inherit transformations</p></li><li><p><code>:inherit_model</code>: Inherit only model transformations</p></li><li><p><code>:inherit_transform_func</code>: Inherit only the transform function</p></li><li><p><code>:nothing</code>: Inherit neither, fully disconnecting the child&#39;s transformations from the parent</p></li></ul><p>Another option is to pass arguments to the <code>transform!()</code> function which then get applied to the plot. For example <code>transformation = (:xz, 1.0)</code> which rotates the <code>xy</code> plane to the <code>xz</code> plane and translates by <code>1.0</code>. For this inheritance defaults to <code>:automatic</code> but can also be set through e.g. <code>(:nothing, (:xz, 1.0))</code>.</p><p><strong><code>transparency</code></strong> = <code>false</code> — Adjusts how the plot deals with transparency. In GLMakie <code>transparency = true</code> results in using Order Independent Transparency.</p><p><strong><code>visible</code></strong> = <code>true</code> — Controls whether the plot gets rendered or not.</p><p><strong><code>width</code></strong> = <code>automatic</code> — The gapless width of the bars. If <code>automatic</code>, the width <code>w</code> is calculated as <code>minimum(diff(sort(unique(positions)))</code>. The actual width of the bars is calculated as <code>w * (1 - gap)</code>.</p>',61)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[62]||(e[62]=[o("a",{href:"https://github.com/MakieOrg/Makie.jl/blob/master/src/recipes.jl#L556-L724",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",C,[o("summary",null,[e[64]||(e[64]=o("a",{id:"TimeseriesMakie.psth!",href:"#TimeseriesMakie.psth!"},[o("span",{class:"jlbinding"},"TimeseriesMakie.psth!")],-1)),e[65]||(e[65]=t()),i(s,{type:"info",class:"jlObjectType jlFunction",text:"Function"})]),e[67]||(e[67]=o("p",null,[o("code",null,"psth!"),t(" is the mutating variant of plotting function "),o("code",null,"psth"),t(". Check the docstring for "),o("code",null,"psth"),t(" for further information.")],-1)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[66]||(e[66]=[o("a",{href:"https://github.com/MakieOrg/Makie.jl/blob/master/src/recipes.jl#L558",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",A,[o("summary",null,[e[68]||(e[68]=o("a",{id:"TimeseriesMakie.ratemap",href:"#TimeseriesMakie.ratemap"},[o("span",{class:"jlbinding"},"TimeseriesMakie.ratemap")],-1)),e[69]||(e[69]=t()),i(s,{type:"info",class:"jlObjectType jlFunction",text:"Function"})]),e[71]||(e[71]=c(`<div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light-ianthina-comments github-dark-ianthina-comments vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ratemap</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(raster; kwargs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ratemap</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(times, raster; kwargs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Per-neuron time-resolved firing rate as a <code>Neuron × Time</code> heatmap. <code>raster</code> is a <code>Neuron × Time</code> matrix: a boolean spike mask or an already-computed rate field. <code>times</code> (optional) supplies the time axis; without it the columns are indexed <code>1:ntime</code>. Time is coarse-grained into bins of <code>binwidth</code> columns, each cell the mean over its bin.</p><p><strong>Key attributes:</strong></p><ul><li><code>binwidth</code> = <code>automatic</code>: number of time columns per bin (<code>automatic</code> = no binning).</li></ul><p><em>Other attributes are shared with <code>Makie.Heatmap</code>.</em></p><p><strong>Plot type</strong></p><p>The plot type alias for the <code>ratemap</code> function is <code>RateMap</code>.</p><p><strong>Attributes</strong></p><p><strong><code>alpha</code></strong> = <code>1.0</code> — The alpha value of the colormap or color attribute. Multiple alphas like in <code>plot(alpha=0.2, color=(:red, 0.5))</code>, will get multiplied.</p><p><strong><code>binwidth</code></strong> = <code>automatic</code> — Number of time columns per bin (<code>automatic</code> = no binning).</p><p><strong><code>clip_planes</code></strong> = <code>@inherit clip_planes automatic</code> — Clip planes offer a way to do clipping in 3D space. You can set a Vector of up to 8 <code>Plane3f</code> planes here, behind which plots will be clipped (i.e. become invisible). By default clip planes are inherited from the parent plot or scene. You can remove parent <code>clip_planes</code> by passing <code>Plane3f[]</code>.</p><p><strong><code>colormap</code></strong> = <code>@inherit colormap :viridis</code> — Sets the colormap that is sampled for numeric <code>color</code>s. <code>PlotUtils.cgrad(...)</code>, <code>Makie.Reverse(any_colormap)</code> can be used as well, or any symbol from ColorBrewer or PlotUtils. To see all available color gradients, you can call <code>Makie.available_gradients()</code>.</p><p><strong><code>colorrange</code></strong> = <code>automatic</code> — The values representing the start and end points of <code>colormap</code>.</p><p><strong><code>colorscale</code></strong> = <code>identity</code> — The color transform function. Can be any function, but only works well together with <code>Colorbar</code> for <code>identity</code>, <code>log</code>, <code>log2</code>, <code>log10</code>, <code>sqrt</code>, <code>logit</code>, <code>Makie.pseudolog10</code>, <code>Makie.Symlog10</code>, <code>Makie.AsinhScale</code>, <code>Makie.SinhScale</code>, <code>Makie.LogScale</code>, <code>Makie.LuptonAsinhScale</code>, and <code>Makie.PowerScale</code>.</p><p><strong><code>depth_shift</code></strong> = <code>0.0</code> — Adjusts the depth value of a plot after all other transformations, i.e. in clip space, where <code>-1 &lt;= depth &lt;= 1</code>. This only applies to GLMakie and WGLMakie and can be used to adjust render order (like a tunable overdraw).</p><p><strong><code>fxaa</code></strong> = <code>true</code> — Adjusts whether the plot is rendered with fxaa (fast approximate anti-aliasing, GLMakie only). Note that some plots implement a better native anti-aliasing solution (scatter, text, lines). For them <code>fxaa = true</code> generally lowers quality. Plots that show smoothly interpolated data (e.g. image, surface) may also degrade in quality as <code>fxaa = true</code> can cause blurring.</p><p><strong><code>highclip</code></strong> = <code>automatic</code> — The color for any value above the colorrange.</p><p><strong><code>inspectable</code></strong> = <code>@inherit inspectable</code> — Sets whether this plot should be seen by <code>DataInspector</code>. The default depends on the theme of the parent scene.</p><p><strong><code>inspector_clear</code></strong> = <code>automatic</code> — Sets a callback function <code>(inspector, plot) -&gt; ...</code> for cleaning up custom indicators in DataInspector.</p><p><strong><code>inspector_hover</code></strong> = <code>automatic</code> — Sets a callback function <code>(inspector, plot, index) -&gt; ...</code> which replaces the default <code>show_data</code> methods.</p><p><strong><code>inspector_label</code></strong> = <code>automatic</code> — Sets a callback function <code>(plot, index, position) -&gt; string</code> which replaces the default label generated by DataInspector.</p><p><strong><code>interpolate</code></strong> = <code>false</code> — Sets whether colors should be interpolated</p><p><strong><code>lowclip</code></strong> = <code>automatic</code> — The color for any value below the colorrange.</p><p><strong><code>model</code></strong> = <code>automatic</code> — Sets a model matrix for the plot. This overrides adjustments made with <code>translate!</code>, <code>rotate!</code> and <code>scale!</code>.</p><p><strong><code>nan_color</code></strong> = <code>:transparent</code> — The color for NaN values.</p><p><strong><code>overdraw</code></strong> = <code>false</code> — Controls if the plot will draw over other plots. This specifically means ignoring depth checks in GL backends</p><p><strong><code>rasterize</code></strong> = <code>@inherit rasterize false</code> — Controls whether CairoMakie rasterizes a plot (true, &gt;0) or not (false, 0). If an integer is given, it sets the pixel density for rasterization.</p><p><strong><code>space</code></strong> = <code>:data</code> — Sets the transformation space for box encompassing the plot. See <code>Makie.spaces()</code> for possible inputs.</p><p><strong><code>ssao</code></strong> = <code>false</code> — Adjusts whether the plot is rendered with ssao (screen space ambient occlusion). Note that this only makes sense in 3D plots and is only applicable with <code>fxaa = true</code>.</p><p><strong><code>transformation</code></strong> = <code>:automatic</code> — Controls the inheritance or directly sets the transformations of a plot. Transformations include the transform function and model matrix as generated by <code>translate!(...)</code>, <code>scale!(...)</code> and <code>rotate!(...)</code>. They can be set directly by passing a <code>Transformation()</code> object or inherited from the parent plot or scene. Inheritance options include:</p><ul><li><p><code>:automatic</code>: Inherit transformations if the parent and child <code>space</code> is compatible</p></li><li><p><code>:inherit</code>: Inherit transformations</p></li><li><p><code>:inherit_model</code>: Inherit only model transformations</p></li><li><p><code>:inherit_transform_func</code>: Inherit only the transform function</p></li><li><p><code>:nothing</code>: Inherit neither, fully disconnecting the child&#39;s transformations from the parent</p></li></ul><p>Another option is to pass arguments to the <code>transform!()</code> function which then get applied to the plot. For example <code>transformation = (:xz, 1.0)</code> which rotates the <code>xy</code> plane to the <code>xz</code> plane and translates by <code>1.0</code>. For this inheritance defaults to <code>:automatic</code> but can also be set through e.g. <code>(:nothing, (:xz, 1.0))</code>.</p><p><strong><code>transparency</code></strong> = <code>false</code> — Adjusts how the plot deals with transparency. In GLMakie <code>transparency = true</code> results in using Order Independent Transparency.</p><p><strong><code>visible</code></strong> = <code>true</code> — Controls whether the plot gets rendered or not.</p>`,34)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[70]||(e[70]=[o("a",{href:"https://github.com/MakieOrg/Makie.jl/blob/master/src/recipes.jl#L556-L648",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",E,[o("summary",null,[e[72]||(e[72]=o("a",{id:"TimeseriesMakie.ratemap!",href:"#TimeseriesMakie.ratemap!"},[o("span",{class:"jlbinding"},"TimeseriesMakie.ratemap!")],-1)),e[73]||(e[73]=t()),i(s,{type:"info",class:"jlObjectType jlFunction",text:"Function"})]),e[75]||(e[75]=o("p",null,[o("code",null,"ratemap!"),t(" is the mutating variant of plotting function "),o("code",null,"ratemap"),t(". Check the docstring for "),o("code",null,"ratemap"),t(" for further information.")],-1)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[74]||(e[74]=[o("a",{href:"https://github.com/MakieOrg/Makie.jl/blob/master/src/recipes.jl#L558",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",I,[o("summary",null,[e[76]||(e[76]=o("a",{id:"TimeseriesMakie.shadows",href:"#TimeseriesMakie.shadows"},[o("span",{class:"jlbinding"},"TimeseriesMakie.shadows")],-1)),e[77]||(e[77]=t()),i(s,{type:"info",class:"jlObjectType jlFunction",text:"Function"})]),e[79]||(e[79]=c('<div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light-ianthina-comments github-dark-ianthina-comments vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">shadows</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x, y, z; kwargs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Plots shadows of a 3D trajectory onto the enclosing axis panes.</p><p><strong>Key attributes:</strong></p><p><code>mode</code> = <code>:projection</code>: The shadowing mode</p><p><code>swapshadows</code> = <code>automatic</code>: Whether to swap the axes for each shadow.</p><p>Can be:</p><ul><li><p><code>true</code> or <code>false</code>: Swap the axes for all shadows from their default values</p></li><li><p><code>NTuple{3, Bool}</code>: Swap the default axes for each shadow individually (x, y, z)</p></li><li><p><code>automatic</code>: Defaults to <code>(true, true, false)</code></p></li></ul><p><code>limits</code> = <code>automatic</code>: The targeted axis limits. To ensure the shadows align with the axes, it is best to provide the <code>Axis</code> limits here. If <code>automatic</code>, the limits are inferred from the data.</p><p><em>Other attributes are shared with <code>Makie.Lines</code>.</em></p><p><strong>Plot type</strong></p><p>The plot type alias for the <code>shadows</code> function is <code>Shadows</code>.</p><p><strong>Attributes</strong></p><p><strong><code>alpha</code></strong> = <code>1.0</code> — The alpha value of the colormap or color attribute. Multiple alphas like in <code>plot(alpha=0.2, color=(:red, 0.5))</code>, will get multiplied.</p><p><strong><code>clip_planes</code></strong> = <code>@inherit clip_planes automatic</code> — Clip planes offer a way to do clipping in 3D space. You can set a Vector of up to 8 <code>Plane3f</code> planes here, behind which plots will be clipped (i.e. become invisible). By default clip planes are inherited from the parent plot or scene. You can remove parent <code>clip_planes</code> by passing <code>Plane3f[]</code>.</p><p><strong><code>color</code></strong> = <code>@inherit linecolor</code> — The color of the line.</p><p><strong><code>colormap</code></strong> = <code>@inherit colormap :viridis</code> — Sets the colormap that is sampled for numeric <code>color</code>s. <code>PlotUtils.cgrad(...)</code>, <code>Makie.Reverse(any_colormap)</code> can be used as well, or any symbol from ColorBrewer or PlotUtils. To see all available color gradients, you can call <code>Makie.available_gradients()</code>.</p><p><strong><code>colorrange</code></strong> = <code>automatic</code> — The values representing the start and end points of <code>colormap</code>.</p><p><strong><code>colorscale</code></strong> = <code>identity</code> — The color transform function. Can be any function, but only works well together with <code>Colorbar</code> for <code>identity</code>, <code>log</code>, <code>log2</code>, <code>log10</code>, <code>sqrt</code>, <code>logit</code>, <code>Makie.pseudolog10</code>, <code>Makie.Symlog10</code>, <code>Makie.AsinhScale</code>, <code>Makie.SinhScale</code>, <code>Makie.LogScale</code>, <code>Makie.LuptonAsinhScale</code>, and <code>Makie.PowerScale</code>.</p><p><strong><code>cycle</code></strong> = <code>[:color]</code> — Sets which attributes to cycle when creating multiple plots. The values to cycle through are defined by the parent Theme. Multiple cycled attributes can be set by passing a vector. Elements can</p><ul><li><p>directly refer to a cycled attribute, e.g. <code>:color</code></p></li><li><p>map a cycled attribute to a palette attribute, e.g. <code>:linecolor =&gt; :color</code></p></li><li><p>map multiple cycled attributes to a palette attribute, e.g. <code>[:linecolor, :markercolor] =&gt; :color</code></p></li></ul><p><strong><code>depth_shift</code></strong> = <code>0.0</code> — Adjusts the depth value of a plot after all other transformations, i.e. in clip space, where <code>-1 &lt;= depth &lt;= 1</code>. This only applies to GLMakie and WGLMakie and can be used to adjust render order (like a tunable overdraw).</p><p><strong><code>fxaa</code></strong> = <code>false</code> — Adjusts whether the plot is rendered with fxaa (fast approximate anti-aliasing, GLMakie only). Note that some plots implement a better native anti-aliasing solution (scatter, text, lines). For them <code>fxaa = true</code> generally lowers quality. Plots that show smoothly interpolated data (e.g. image, surface) may also degrade in quality as <code>fxaa = true</code> can cause blurring.</p><p><strong><code>highclip</code></strong> = <code>automatic</code> — The color for any value above the colorrange.</p><p><strong><code>inspectable</code></strong> = <code>@inherit inspectable</code> — Sets whether this plot should be seen by <code>DataInspector</code>. The default depends on the theme of the parent scene.</p><p><strong><code>inspector_clear</code></strong> = <code>automatic</code> — Sets a callback function <code>(inspector, plot) -&gt; ...</code> for cleaning up custom indicators in DataInspector.</p><p><strong><code>inspector_hover</code></strong> = <code>automatic</code> — Sets a callback function <code>(inspector, plot, index) -&gt; ...</code> which replaces the default <code>show_data</code> methods.</p><p><strong><code>inspector_label</code></strong> = <code>automatic</code> — Sets a callback function <code>(plot, index, position) -&gt; string</code> which replaces the default label generated by DataInspector.</p><p><strong><code>joinstyle</code></strong> = <code>@inherit joinstyle</code> — Controls the rendering at corners. Options are <code>:miter</code> for sharp corners, <code>:bevel</code> for &quot;cut off&quot; corners, and <code>:round</code> for rounded corners. If the corner angle is below <code>miter_limit</code>, <code>:miter</code> is equivalent to <code>:bevel</code> to avoid long spikes.</p><p><strong><code>limits</code></strong> = <code>automatic</code> — The targeted axis limits</p><p><strong><code>linecap</code></strong> = <code>@inherit linecap</code> — Sets the type of line cap used. Options are <code>:butt</code> (flat without extrusion), <code>:square</code> (flat with half a linewidth extrusion) or <code>:round</code>.</p><p><strong><code>linestyle</code></strong> = <code>nothing</code> — Sets the dash pattern of the line. Options are <code>:solid</code> (equivalent to <code>nothing</code>), <code>:dot</code>, <code>:dash</code>, <code>:dashdot</code> and <code>:dashdotdot</code>. These can also be given in a tuple with a gap style modifier, either <code>:normal</code>, <code>:dense</code> or <code>:loose</code>. For example, <code>(:dot, :loose)</code> or <code>(:dashdot, :dense)</code>.</p><p>For custom patterns have a look at <a href="./@ref"><code>Makie.Linestyle</code></a>.</p><p><strong><code>linewidth</code></strong> = <code>@inherit linewidth</code> — Sets the width of the line in screen units</p><p><strong><code>lowclip</code></strong> = <code>automatic</code> — The color for any value below the colorrange.</p><p><strong><code>miter_limit</code></strong> = <code>@inherit miter_limit</code> — Sets the minimum inner join angle below which miter joins truncate. See also <code>Makie.miter_distance_to_angle</code>.</p><p><strong><code>mode</code></strong> = <code>:projection</code> — The shadowing mode</p><p><strong><code>model</code></strong> = <code>automatic</code> — Sets a model matrix for the plot. This overrides adjustments made with <code>translate!</code>, <code>rotate!</code> and <code>scale!</code>.</p><p><strong><code>nan_color</code></strong> = <code>:transparent</code> — The color for NaN values.</p><p><strong><code>overdraw</code></strong> = <code>false</code> — Controls if the plot will draw over other plots. This specifically means ignoring depth checks in GL backends</p><p><strong><code>rasterize</code></strong> = <code>@inherit rasterize false</code> — Controls whether CairoMakie rasterizes a plot (true, &gt;0) or not (false, 0). If an integer is given, it sets the pixel density for rasterization.</p><p><strong><code>space</code></strong> = <code>:data</code> — Sets the transformation space for box encompassing the plot. See <code>Makie.spaces()</code> for possible inputs.</p><p><strong><code>ssao</code></strong> = <code>false</code> — Adjusts whether the plot is rendered with ssao (screen space ambient occlusion). Note that this only makes sense in 3D plots and is only applicable with <code>fxaa = true</code>.</p><p><strong><code>swapshadows</code></strong> = <code>automatic</code> — Whether to swap the axes for each shadow</p><p><strong><code>transformation</code></strong> = <code>:automatic</code> — Controls the inheritance or directly sets the transformations of a plot. Transformations include the transform function and model matrix as generated by <code>translate!(...)</code>, <code>scale!(...)</code> and <code>rotate!(...)</code>. They can be set directly by passing a <code>Transformation()</code> object or inherited from the parent plot or scene. Inheritance options include:</p><ul><li><p><code>:automatic</code>: Inherit transformations if the parent and child <code>space</code> is compatible</p></li><li><p><code>:inherit</code>: Inherit transformations</p></li><li><p><code>:inherit_model</code>: Inherit only model transformations</p></li><li><p><code>:inherit_transform_func</code>: Inherit only the transform function</p></li><li><p><code>:nothing</code>: Inherit neither, fully disconnecting the child&#39;s transformations from the parent</p></li></ul><p>Another option is to pass arguments to the <code>transform!()</code> function which then get applied to the plot. For example <code>transformation = (:xz, 1.0)</code> which rotates the <code>xy</code> plane to the <code>xz</code> plane and translates by <code>1.0</code>. For this inheritance defaults to <code>:automatic</code> but can also be set through e.g. <code>(:nothing, (:xz, 1.0))</code>.</p><p><strong><code>transparency</code></strong> = <code>false</code> — Adjusts how the plot deals with transparency. In GLMakie <code>transparency = true</code> results in using Order Independent Transparency.</p><p><strong><code>visible</code></strong> = <code>true</code> — Controls whether the plot gets rendered or not.</p>',48)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[78]||(e[78]=[o("a",{href:"https://github.com/MakieOrg/Makie.jl/blob/master/src/recipes.jl#L556-L689",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",F,[o("summary",null,[e[80]||(e[80]=o("a",{id:"TimeseriesMakie.shadows!",href:"#TimeseriesMakie.shadows!"},[o("span",{class:"jlbinding"},"TimeseriesMakie.shadows!")],-1)),e[81]||(e[81]=t()),i(s,{type:"info",class:"jlObjectType jlFunction",text:"Function"})]),e[83]||(e[83]=o("p",null,[o("code",null,"shadows!"),t(" is the mutating variant of plotting function "),o("code",null,"shadows"),t(". Check the docstring for "),o("code",null,"shadows"),t(" for further information.")],-1)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[82]||(e[82]=[o("a",{href:"https://github.com/MakieOrg/Makie.jl/blob/master/src/recipes.jl#L558",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",P,[o("summary",null,[e[84]||(e[84]=o("a",{id:"TimeseriesMakie.spectrumplot",href:"#TimeseriesMakie.spectrumplot"},[o("span",{class:"jlbinding"},"TimeseriesMakie.spectrumplot")],-1)),e[85]||(e[85]=t()),i(s,{type:"info",class:"jlObjectType jlFunction",text:"Function"})]),e[87]||(e[87]=c(`<div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light-ianthina-comments github-dark-ianthina-comments vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">spectrumplot</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x; kwargs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">spectrumplot</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, s; kwargs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Plot a power spectrum, optionally marking and labelling its prominent peaks. Requires the <code>TimeseriesTools</code> extension (<code>using TimeseriesTools</code>).</p><p><code>x</code> may be a <code>UnivariateSpectrum</code>, a <code>MultivariateSpectrum</code> (reduced to a line and a spread band), an <code>AbstractTimeseries</code> (its <code>spectrum</code> is taken first), or a vector of <code>Point2</code>. Given two arguments, <code>f</code> holds the frequencies and <code>s</code> the spectral density.</p><p><strong>Key attributes:</strong></p><ul><li><p><code>peaks</code> = <code>false</code>: mark prominent peaks. <code>true</code> marks every one found, an <code>Int</code> the <code>n</code> most prominent, and a <code>Real</code> those whose prominence exceeds that fraction of their height.</p></li><li><p><code>pwindow</code> = <code>10</code>: the neighbourhood, in samples, over which a peak must be maximal. A noisy or finely resolved spectrum usually needs a smaller value than the default.</p></li><li><p><code>annotate</code> = <code>false</code>: label the marked peaks. <code>true</code> prints their coordinates; a function, or a tuple of one per coordinate, formats them. <code>textformat</code> post-processes that tuple.</p></li><li><p><code>nonnegative</code> = <code>true</code>: drop non-positive frequencies and densities, which a log axis cannot show.</p></li><li><p><code>average</code> = <code>median</code> and <code>width</code> = <code>(q₀.₂₅, q₀.₇₅)</code>: how a multivariate spectrum is reduced to a line and a band. <code>width</code> takes a number (a symmetric width), a pair of numbers, a function of each frequency&#39;s values, or a pair of such functions. <code>bandalpha</code> and <code>bandcolor</code> style the result.</p></li></ul><p><em>Other attributes are shared with <code>Makie.Lines</code>, <code>Makie.Scatter</code>, <code>Makie.Band</code> and <code>Makie.Text</code>.</em></p><p>See also <a href="/TimeseriesDocs.jl/dev/TimeseriesMakie/reference#TimeseriesMakie.plotspectrum"><code>plotspectrum</code></a>, which sets log axes, limits and unit-aware labels to match.</p>`,7)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[86]||(e[86]=[o("a",{href:"https://github.com/brendanjohnharris/TimeseriesMakie.jl/blob/main/src/TimeseriesMakie.jl#L66-L100",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",L,[o("summary",null,[e[88]||(e[88]=o("a",{id:"TimeseriesMakie.spikeraster",href:"#TimeseriesMakie.spikeraster"},[o("span",{class:"jlbinding"},"TimeseriesMakie.spikeraster")],-1)),e[89]||(e[89]=t()),i(s,{type:"info",class:"jlObjectType jlFunction",text:"Function"})]),e[91]||(e[91]=c('<div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light-ianthina-comments github-dark-ianthina-comments vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">spikeraster</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(times, ids; kwargs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Scatter a spike raster: one marker per spike at <code>(time, neuron)</code>, where <code>times[k]</code> and <code>ids[k]</code> are the time and (integer) neuron index of the <code>k</code>-th spike.</p><p>Alternative single-argument inputs are accepted via <code>convert_arguments</code>:</p><ul><li><p>a vector of per-neuron spike-time vectors (<code>spikes[n]</code> = the spike times of neuron <code>n</code>);</p></li><li><p>a <code>Neuron × Time</code> boolean matrix (a spike mask; columns index time).</p></li></ul><p><strong>Key attributes:</strong></p><ul><li><p><code>sortby</code> = <code>false</code>: reorder neurons on the y-axis. <code>false</code> keeps the id order; <code>:rate</code> sorts by spike count; a <code>Function</code> maps each neuron&#39;s spike-time vector to a sort key; a vector supplies an explicit per-neuron key.</p></li><li><p><code>rev</code> = <code>false</code>: reverse the sort order.</p></li></ul><p><em>Other attributes are shared with <code>Makie.Scatter</code>.</em></p><p><strong>Plot type</strong></p><p>The plot type alias for the <code>spikeraster</code> function is <code>SpikeRaster</code>.</p><p><strong>Attributes</strong></p><p><strong><code>alpha</code></strong> = <code>1.0</code> — The alpha value of the colormap or color attribute. Multiple alphas like in <code>plot(alpha=0.2, color=(:red, 0.5))</code>, will get multiplied.</p><p><strong><code>clip_planes</code></strong> = <code>@inherit clip_planes automatic</code> — Clip planes offer a way to do clipping in 3D space. You can set a Vector of up to 8 <code>Plane3f</code> planes here, behind which plots will be clipped (i.e. become invisible). By default clip planes are inherited from the parent plot or scene. You can remove parent <code>clip_planes</code> by passing <code>Plane3f[]</code>.</p><p><strong><code>color</code></strong> = <code>@inherit markercolor</code> — Sets the color of the marker. If no color is set, multiple calls to <code>scatter!</code> will cycle through the axis color palette.</p><p><strong><code>colormap</code></strong> = <code>@inherit colormap :viridis</code> — Sets the colormap that is sampled for numeric <code>color</code>s. <code>PlotUtils.cgrad(...)</code>, <code>Makie.Reverse(any_colormap)</code> can be used as well, or any symbol from ColorBrewer or PlotUtils. To see all available color gradients, you can call <code>Makie.available_gradients()</code>.</p><p><strong><code>colorrange</code></strong> = <code>automatic</code> — The values representing the start and end points of <code>colormap</code>.</p><p><strong><code>colorscale</code></strong> = <code>identity</code> — The color transform function. Can be any function, but only works well together with <code>Colorbar</code> for <code>identity</code>, <code>log</code>, <code>log2</code>, <code>log10</code>, <code>sqrt</code>, <code>logit</code>, <code>Makie.pseudolog10</code>, <code>Makie.Symlog10</code>, <code>Makie.AsinhScale</code>, <code>Makie.SinhScale</code>, <code>Makie.LogScale</code>, <code>Makie.LuptonAsinhScale</code>, and <code>Makie.PowerScale</code>.</p><p><strong><code>cycle</code></strong> = <code>[:color]</code> — Sets which attributes to cycle when creating multiple plots. The values to cycle through are defined by the parent Theme. Multiple cycled attributes can be set by passing a vector. Elements can</p><ul><li><p>directly refer to a cycled attribute, e.g. <code>:color</code></p></li><li><p>map a cycled attribute to a palette attribute, e.g. <code>:linecolor =&gt; :color</code></p></li><li><p>map multiple cycled attributes to a palette attribute, e.g. <code>[:linecolor, :markercolor] =&gt; :color</code></p></li></ul><p><strong><code>depth_shift</code></strong> = <code>0.0</code> — Adjusts the depth value of a plot after all other transformations, i.e. in clip space, where <code>-1 &lt;= depth &lt;= 1</code>. This only applies to GLMakie and WGLMakie and can be used to adjust render order (like a tunable overdraw).</p><p><strong><code>depthsorting</code></strong> = <code>false</code> — Enables depth-sorting of markers which can improve border artifacts. Currently supported in GLMakie only.</p><p><strong><code>distancefield</code></strong> = <code>nothing</code> — Optional distancefield used for e.g. font and bezier path rendering. Will get set automatically.</p><p><strong><code>font</code></strong> = <code>&quot;default&quot;</code> — Sets the font to be used for character markers</p><p><strong><code>fxaa</code></strong> = <code>false</code> — Adjusts whether the plot is rendered with fxaa (fast approximate anti-aliasing, GLMakie only). Note that some plots implement a better native anti-aliasing solution (scatter, text, lines). For them <code>fxaa = true</code> generally lowers quality. Plots that show smoothly interpolated data (e.g. image, surface) may also degrade in quality as <code>fxaa = true</code> can cause blurring.</p><p><strong><code>glowcolor</code></strong> = <code>(:black, 0.0)</code> — Sets the color of the glow effect around the marker.</p><p><strong><code>glowwidth</code></strong> = <code>0.0</code> — Sets the size of a glow effect around the marker.</p><p><strong><code>highclip</code></strong> = <code>automatic</code> — The color for any value above the colorrange.</p><p><strong><code>inspectable</code></strong> = <code>@inherit inspectable</code> — Sets whether this plot should be seen by <code>DataInspector</code>. The default depends on the theme of the parent scene.</p><p><strong><code>inspector_clear</code></strong> = <code>automatic</code> — Sets a callback function <code>(inspector, plot) -&gt; ...</code> for cleaning up custom indicators in DataInspector.</p><p><strong><code>inspector_hover</code></strong> = <code>automatic</code> — Sets a callback function <code>(inspector, plot, index) -&gt; ...</code> which replaces the default <code>show_data</code> methods.</p><p><strong><code>inspector_label</code></strong> = <code>automatic</code> — Sets a callback function <code>(plot, index, position) -&gt; string</code> which replaces the default label generated by DataInspector.</p><p><strong><code>lowclip</code></strong> = <code>automatic</code> — The color for any value below the colorrange.</p><p><strong><code>marker</code></strong> = <code>@inherit marker</code> — Sets the scatter marker.</p><p><strong><code>marker_offset</code></strong> = <code>Vec3f(0)</code> — The offset of the marker from the given position in <code>markerspace</code> units. An offset of 0 corresponds to a centered marker.</p><p><strong><code>markersize</code></strong> = <code>4</code> — Marker size (defaults smaller than <code>Scatter</code> for dense rasters).</p><p><strong><code>markerspace</code></strong> = <code>:pixel</code> — Sets the space in which <code>markersize</code> is given. See <code>Makie.spaces()</code> for possible inputs</p><p><strong><code>model</code></strong> = <code>automatic</code> — Sets a model matrix for the plot. This overrides adjustments made with <code>translate!</code>, <code>rotate!</code> and <code>scale!</code>.</p><p><strong><code>nan_color</code></strong> = <code>:transparent</code> — The color for NaN values.</p><p><strong><code>overdraw</code></strong> = <code>false</code> — Controls if the plot will draw over other plots. This specifically means ignoring depth checks in GL backends</p><p><strong><code>rasterize</code></strong> = <code>@inherit rasterize false</code> — Controls whether CairoMakie rasterizes a plot (true, &gt;0) or not (false, 0). If an integer is given, it sets the pixel density for rasterization.</p><p><strong><code>rev</code></strong> = <code>false</code> — Reverse the sort order.</p><p><strong><code>rotation</code></strong> = <code>Billboard()</code> — Sets the rotation of the marker. A <code>Billboard</code> rotation is always around the depth axis.</p><p><strong><code>sortby</code></strong> = <code>false</code> — How to order neurons on the y-axis: <code>false</code> (id order), <code>:rate</code>, a function of a neuron&#39;s spike-time vector, or a vector of per-neuron sort keys.</p><p><strong><code>space</code></strong> = <code>:data</code> — Sets the transformation space for box encompassing the plot. See <code>Makie.spaces()</code> for possible inputs.</p><p><strong><code>ssao</code></strong> = <code>false</code> — Adjusts whether the plot is rendered with ssao (screen space ambient occlusion). Note that this only makes sense in 3D plots and is only applicable with <code>fxaa = true</code>.</p><p><strong><code>strokecolor</code></strong> = <code>@inherit markerstrokecolor</code> — Sets the color of the outline around a marker.</p><p><strong><code>strokewidth</code></strong> = <code>@inherit markerstrokewidth</code> — Sets the width of the outline around a marker.</p><p><strong><code>transform_marker</code></strong> = <code>false</code> — Controls whether the model matrix (without translation) applies to the marker itself, rather than just the positions. (If this is true, <code>scale!</code> and <code>rotate!</code> will affect the marker.</p><p><strong><code>transformation</code></strong> = <code>:automatic</code> — Controls the inheritance or directly sets the transformations of a plot. Transformations include the transform function and model matrix as generated by <code>translate!(...)</code>, <code>scale!(...)</code> and <code>rotate!(...)</code>. They can be set directly by passing a <code>Transformation()</code> object or inherited from the parent plot or scene. Inheritance options include:</p><ul><li><p><code>:automatic</code>: Inherit transformations if the parent and child <code>space</code> is compatible</p></li><li><p><code>:inherit</code>: Inherit transformations</p></li><li><p><code>:inherit_model</code>: Inherit only model transformations</p></li><li><p><code>:inherit_transform_func</code>: Inherit only the transform function</p></li><li><p><code>:nothing</code>: Inherit neither, fully disconnecting the child&#39;s transformations from the parent</p></li></ul><p>Another option is to pass arguments to the <code>transform!()</code> function which then get applied to the plot. For example <code>transformation = (:xz, 1.0)</code> which rotates the <code>xy</code> plane to the <code>xz</code> plane and translates by <code>1.0</code>. For this inheritance defaults to <code>:automatic</code> but can also be set through e.g. <code>(:nothing, (:xz, 1.0))</code>.</p><p><strong><code>transparency</code></strong> = <code>false</code> — Adjusts how the plot deals with transparency. In GLMakie <code>transparency = true</code> results in using Order Independent Transparency.</p><p><strong><code>visible</code></strong> = <code>true</code> — Controls whether the plot gets rendered or not.</p>',52)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[90]||(e[90]=[o("a",{href:"https://github.com/MakieOrg/Makie.jl/blob/master/src/recipes.jl#L556-L691",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",O,[o("summary",null,[e[92]||(e[92]=o("a",{id:"TimeseriesMakie.spikeraster!",href:"#TimeseriesMakie.spikeraster!"},[o("span",{class:"jlbinding"},"TimeseriesMakie.spikeraster!")],-1)),e[93]||(e[93]=t()),i(s,{type:"info",class:"jlObjectType jlFunction",text:"Function"})]),e[95]||(e[95]=o("p",null,[o("code",null,"spikeraster!"),t(" is the mutating variant of plotting function "),o("code",null,"spikeraster"),t(". Check the docstring for "),o("code",null,"spikeraster"),t(" for further information.")],-1)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[94]||(e[94]=[o("a",{href:"https://github.com/MakieOrg/Makie.jl/blob/master/src/recipes.jl#L558",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",D,[o("summary",null,[e[96]||(e[96]=o("a",{id:"TimeseriesMakie.strokemesh-Tuple{AbstractVector{<:Point2}, AbstractVector}",href:"#TimeseriesMakie.strokemesh-Tuple{AbstractVector{<:Point2}, AbstractVector}"},[o("span",{class:"jlbinding"},"TimeseriesMakie.strokemesh")],-1)),e[97]||(e[97]=t()),i(s,{type:"info",class:"jlObjectType jlMethod",text:"Method"})]),e[99]||(e[99]=c('<div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light-ianthina-comments github-dark-ianthina-comments vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">strokemesh</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(P, w) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (vertices, faces, source)</span></span></code></pre></div><p>Triangle mesh for a variable-width line through the pixel-space points <code>P</code>, <code>w[i]</code> pixels wide at <code>P[i]</code>. Each segment is a quad tapering linearly between its end widths; a vertex gets a fan only where the path turns enough to open a visible gap on the outside, and the ends get round caps. Triangles overdraw rather than being subtracted by a fill rule, so a turn tighter than the half-width stays solid. <code>NaN</code> points break the stroke without a cap. <code>source[k]</code> is the index in <code>P</code> that vertex <code>k</code> belongs to, for expanding per-point colours.</p>',2)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[98]||(e[98]=[o("a",{href:"https://github.com/brendanjohnharris/TimeseriesMakie.jl/blob/main/src/recipes/kinetic.jl#L269-L278",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",z,[o("summary",null,[e[100]||(e[100]=o("a",{id:"TimeseriesMakie.strokeoutline-Tuple{AbstractVector{<:Point2}, AbstractVector}",href:"#TimeseriesMakie.strokeoutline-Tuple{AbstractVector{<:Point2}, AbstractVector}"},[o("span",{class:"jlbinding"},"TimeseriesMakie.strokeoutline")],-1)),e[101]||(e[101]=t()),i(s,{type:"info",class:"jlObjectType jlMethod",text:"Method"})]),e[103]||(e[103]=c('<div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light-ianthina-comments github-dark-ianthina-comments vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">strokeoutline</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(P, w) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Vector{Vector{Point2f}}</span></span></code></pre></div><p>Closed outlines for a variable-width stroke through the pixel-space points <code>P</code>, <code>w[i]</code> pixels wide at <code>P[i]</code>. One ring per run of finite points: up one offset, round the far cap, back down the other, round the near cap, with a fan at each vertex on whichever side the turn opens a gap.</p><p>Filled by the nonzero winding rule, a ring that crosses itself fills its union, so a turn tighter than the half width, or a path that doubles back over itself, stays solid.</p><p>This is <a href="/TimeseriesDocs.jl/dev/TimeseriesMakie/reference#TimeseriesMakie.strokemesh-Tuple{AbstractVector{&lt;:Point2}, AbstractVector}"><code>strokemesh</code></a> without the interior edges, which is what lets CairoMakie anti-alias it: it paints a mesh through a Cairo mesh pattern, which is not anti-aliased, while a filled path is. The cost is that one ring carries one colour.</p>',4)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[102]||(e[102]=[o("a",{href:"https://github.com/brendanjohnharris/TimeseriesMakie.jl/blob/main/src/recipes/kinetic.jl#L338-L351",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",N,[o("summary",null,[e[104]||(e[104]=o("a",{id:"TimeseriesMakie.traces",href:"#TimeseriesMakie.traces"},[o("span",{class:"jlbinding"},"TimeseriesMakie.traces")],-1)),e[105]||(e[105]=t()),i(s,{type:"info",class:"jlObjectType jlFunction",text:"Function"})]),e[107]||(e[107]=c('<div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light-ianthina-comments github-dark-ianthina-comments vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">traces</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x, y, Z; kwargs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Plot the columns of <code>Z</code> over the domain <code>x</code>, colored by <code>y</code>.</p><p><strong>Key attributes:</strong></p><ul><li><p><code>linecolor</code> = <code>automatic</code>: Sets the color of the traces.</p></li><li><p><code>spacing</code> = <code>0</code>: The spacing between traces.</p></li></ul><p>Can be a number in data space, or one of the following modes: - <code>:even</code>: Even spacing equal to the greatest difference between traces. - <code>:close</code>: Successive traces are spaced by the smallest difference between them.</p><ul><li><code>offset</code> = <code>1</code>: The offset factor (offset * spacing)</li></ul><p><em>Other attributes are shared with <code>Makie.Lines</code>.</em></p><p><strong>Plot type</strong></p><p>The plot type alias for the <code>traces</code> function is <code>Traces</code>.</p><p><strong>Attributes</strong></p><p><strong><code>alpha</code></strong> = <code>1.0</code> — The alpha value of the colormap or color attribute. Multiple alphas like in <code>plot(alpha=0.2, color=(:red, 0.5))</code>, will get multiplied.</p><p><strong><code>clip_planes</code></strong> = <code>@inherit clip_planes automatic</code> — Clip planes offer a way to do clipping in 3D space. You can set a Vector of up to 8 <code>Plane3f</code> planes here, behind which plots will be clipped (i.e. become invisible). By default clip planes are inherited from the parent plot or scene. You can remove parent <code>clip_planes</code> by passing <code>Plane3f[]</code>.</p><p><strong><code>colormap</code></strong> = <code>@inherit colormap :viridis</code> — Sets the colormap that is sampled for numeric <code>color</code>s. <code>PlotUtils.cgrad(...)</code>, <code>Makie.Reverse(any_colormap)</code> can be used as well, or any symbol from ColorBrewer or PlotUtils. To see all available color gradients, you can call <code>Makie.available_gradients()</code>.</p><p><strong><code>colorrange</code></strong> = <code>automatic</code> — The values representing the start and end points of <code>colormap</code>.</p><p><strong><code>colorscale</code></strong> = <code>identity</code> — The color transform function. Can be any function, but only works well together with <code>Colorbar</code> for <code>identity</code>, <code>log</code>, <code>log2</code>, <code>log10</code>, <code>sqrt</code>, <code>logit</code>, <code>Makie.pseudolog10</code>, <code>Makie.Symlog10</code>, <code>Makie.AsinhScale</code>, <code>Makie.SinhScale</code>, <code>Makie.LogScale</code>, <code>Makie.LuptonAsinhScale</code>, and <code>Makie.PowerScale</code>.</p><p><strong><code>cycle</code></strong> = <code>[:color]</code> — Sets which attributes to cycle when creating multiple plots. The values to cycle through are defined by the parent Theme. Multiple cycled attributes can be set by passing a vector. Elements can</p><ul><li><p>directly refer to a cycled attribute, e.g. <code>:color</code></p></li><li><p>map a cycled attribute to a palette attribute, e.g. <code>:linecolor =&gt; :color</code></p></li><li><p>map multiple cycled attributes to a palette attribute, e.g. <code>[:linecolor, :markercolor] =&gt; :color</code></p></li></ul><p><strong><code>depth_shift</code></strong> = <code>0.0</code> — Adjusts the depth value of a plot after all other transformations, i.e. in clip space, where <code>-1 &lt;= depth &lt;= 1</code>. This only applies to GLMakie and WGLMakie and can be used to adjust render order (like a tunable overdraw).</p><p><strong><code>fxaa</code></strong> = <code>false</code> — Adjusts whether the plot is rendered with fxaa (fast approximate anti-aliasing, GLMakie only). Note that some plots implement a better native anti-aliasing solution (scatter, text, lines). For them <code>fxaa = true</code> generally lowers quality. Plots that show smoothly interpolated data (e.g. image, surface) may also degrade in quality as <code>fxaa = true</code> can cause blurring.</p><p><strong><code>highclip</code></strong> = <code>automatic</code> — The color for any value above the colorrange.</p><p><strong><code>inspectable</code></strong> = <code>@inherit inspectable</code> — Sets whether this plot should be seen by <code>DataInspector</code>. The default depends on the theme of the parent scene.</p><p><strong><code>inspector_clear</code></strong> = <code>automatic</code> — Sets a callback function <code>(inspector, plot) -&gt; ...</code> for cleaning up custom indicators in DataInspector.</p><p><strong><code>inspector_hover</code></strong> = <code>automatic</code> — Sets a callback function <code>(inspector, plot, index) -&gt; ...</code> which replaces the default <code>show_data</code> methods.</p><p><strong><code>inspector_label</code></strong> = <code>automatic</code> — Sets a callback function <code>(plot, index, position) -&gt; string</code> which replaces the default label generated by DataInspector.</p><p><strong><code>joinstyle</code></strong> = <code>@inherit joinstyle</code> — Controls the rendering at corners. Options are <code>:miter</code> for sharp corners, <code>:bevel</code> for &quot;cut off&quot; corners, and <code>:round</code> for rounded corners. If the corner angle is below <code>miter_limit</code>, <code>:miter</code> is equivalent to <code>:bevel</code> to avoid long spikes.</p><p><strong><code>linecap</code></strong> = <code>@inherit linecap</code> — Sets the type of line cap used. Options are <code>:butt</code> (flat without extrusion), <code>:square</code> (flat with half a linewidth extrusion) or <code>:round</code>.</p><p><strong><code>linecolor</code></strong> = <code>automatic</code> — <em>No docs available.</em></p><p><strong><code>linestyle</code></strong> = <code>nothing</code> — Sets the dash pattern of the line. Options are <code>:solid</code> (equivalent to <code>nothing</code>), <code>:dot</code>, <code>:dash</code>, <code>:dashdot</code> and <code>:dashdotdot</code>. These can also be given in a tuple with a gap style modifier, either <code>:normal</code>, <code>:dense</code> or <code>:loose</code>. For example, <code>(:dot, :loose)</code> or <code>(:dashdot, :dense)</code>.</p><p>For custom patterns have a look at <a href="./@ref"><code>Makie.Linestyle</code></a>.</p><p><strong><code>linewidth</code></strong> = <code>@inherit linewidth</code> — Sets the width of the line in screen units</p><p><strong><code>lowclip</code></strong> = <code>automatic</code> — The color for any value below the colorrange.</p><p><strong><code>miter_limit</code></strong> = <code>@inherit miter_limit</code> — Sets the minimum inner join angle below which miter joins truncate. See also <code>Makie.miter_distance_to_angle</code>.</p><p><strong><code>model</code></strong> = <code>automatic</code> — Sets a model matrix for the plot. This overrides adjustments made with <code>translate!</code>, <code>rotate!</code> and <code>scale!</code>.</p><p><strong><code>nan_color</code></strong> = <code>:transparent</code> — The color for NaN values.</p><p><strong><code>offset</code></strong> = <code>1</code> — <em>No docs available.</em></p><p><strong><code>overdraw</code></strong> = <code>false</code> — Controls if the plot will draw over other plots. This specifically means ignoring depth checks in GL backends</p><p><strong><code>rasterize</code></strong> = <code>@inherit rasterize false</code> — Controls whether CairoMakie rasterizes a plot (true, &gt;0) or not (false, 0). If an integer is given, it sets the pixel density for rasterization.</p><p><strong><code>space</code></strong> = <code>:data</code> — Sets the transformation space for box encompassing the plot. See <code>Makie.spaces()</code> for possible inputs.</p><p><strong><code>spacing</code></strong> = <code>0</code> — <em>No docs available.</em></p><p><strong><code>ssao</code></strong> = <code>false</code> — Adjusts whether the plot is rendered with ssao (screen space ambient occlusion). Note that this only makes sense in 3D plots and is only applicable with <code>fxaa = true</code>.</p><p><strong><code>transformation</code></strong> = <code>:automatic</code> — Controls the inheritance or directly sets the transformations of a plot. Transformations include the transform function and model matrix as generated by <code>translate!(...)</code>, <code>scale!(...)</code> and <code>rotate!(...)</code>. They can be set directly by passing a <code>Transformation()</code> object or inherited from the parent plot or scene. Inheritance options include:</p><ul><li><p><code>:automatic</code>: Inherit transformations if the parent and child <code>space</code> is compatible</p></li><li><p><code>:inherit</code>: Inherit transformations</p></li><li><p><code>:inherit_model</code>: Inherit only model transformations</p></li><li><p><code>:inherit_transform_func</code>: Inherit only the transform function</p></li><li><p><code>:nothing</code>: Inherit neither, fully disconnecting the child&#39;s transformations from the parent</p></li></ul><p>Another option is to pass arguments to the <code>transform!()</code> function which then get applied to the plot. For example <code>transformation = (:xz, 1.0)</code> which rotates the <code>xy</code> plane to the <code>xz</code> plane and translates by <code>1.0</code>. For this inheritance defaults to <code>:automatic</code> but can also be set through e.g. <code>(:nothing, (:xz, 1.0))</code>.</p><p><strong><code>transparency</code></strong> = <code>false</code> — Adjusts how the plot deals with transparency. In GLMakie <code>transparency = true</code> results in using Order Independent Transparency.</p><p><strong><code>visible</code></strong> = <code>true</code> — Controls whether the plot gets rendered or not.</p>',45)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[106]||(e[106]=[o("a",{href:"https://github.com/MakieOrg/Makie.jl/blob/master/src/recipes.jl#L556-L682",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",q,[o("summary",null,[e[108]||(e[108]=o("a",{id:"TimeseriesMakie.traces!",href:"#TimeseriesMakie.traces!"},[o("span",{class:"jlbinding"},"TimeseriesMakie.traces!")],-1)),e[109]||(e[109]=t()),i(s,{type:"info",class:"jlObjectType jlFunction",text:"Function"})]),e[111]||(e[111]=o("p",null,[o("code",null,"traces!"),t(" is the mutating variant of plotting function "),o("code",null,"traces"),t(". Check the docstring for "),o("code",null,"traces"),t(" for further information.")],-1)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[110]||(e[110]=[o("a",{href:"https://github.com/MakieOrg/Makie.jl/blob/master/src/recipes.jl#L558",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",V,[o("summary",null,[e[112]||(e[112]=o("a",{id:"TimeseriesMakie.trail",href:"#TimeseriesMakie.trail"},[o("span",{class:"jlbinding"},"TimeseriesMakie.trail")],-1)),e[113]||(e[113]=t()),i(s,{type:"info",class:"jlObjectType jlFunction",text:"Function"})]),e[115]||(e[115]=c('<div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light-ianthina-comments github-dark-ianthina-comments vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">trail</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x, y; kwargs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Plot a fading trace of points in 2D or 3D space.</p><p><strong>Key attributes:</strong></p><p><code>n_points</code> = <code>automatic</code>: Fixes the length of the trail. By default, this is equal to the length of <code>x</code> and <code>y</code>. If <code>n_points</code> is less than the length of <code>x</code> and <code>y</code>, the last <code>n_points</code> will be plotted.</p><p><code>linecolor</code> = <code>@inherit linecolor</code>: Sets the color of the trail. Should be a single color (e.g. &quot;red&quot;, :red, (:red, 0.2), RGBA(0.1, 0.2, 0.3, 0.4)). This value is overridden by <code>color</code></p><p><code>color</code> = <code>nothing</code>: Specifies the color values for the trail.</p><p>If <code>!isnothing(color)</code>, trail colors will be sampled from the <code>colormap</code> depending on the value of <code>color</code>. <code>color</code> can be:</p><ul><li><p>A collection of numbers representing values to be sampled from the colormap.</p></li><li><p>A function of the index of a point in the trail (e.g. <code>Base.Fix2(^, 3)</code>).</p></li></ul><p><code>colormap</code> = <code>@inherit colormap</code>: Specifies the colormap to use for the trail when <code>!isnothing(color)</code>.</p><p><code>alpha</code> = <code>identity</code>: Controls the transparency profile of the trail. <code>alpha</code> can be:</p><ul><li><p>A single number (e.g. <code>0.5</code>).</p></li><li><p>A function of the index of a point in the trail (e.g. <code>Base.Fix2(^, 3)</code>).</p></li><li><p>A collection of numbers representing alpha values for each point in the trail.</p></li></ul><p>To sidestep alpha normalization, explicitly pass a vector of alpha values.</p><p><em>Other attributes are shared with <code>Makie.Lines</code>.</em></p><p><strong>Plot type</strong></p><p>The plot type alias for the <code>trail</code> function is <code>Trail</code>.</p><p><strong>Attributes</strong></p><p><strong><code>alpha</code></strong> = <code>identity</code> — <em>No docs available.</em></p><p><strong><code>clip_planes</code></strong> = <code>@inherit clip_planes automatic</code> — Clip planes offer a way to do clipping in 3D space. You can set a Vector of up to 8 <code>Plane3f</code> planes here, behind which plots will be clipped (i.e. become invisible). By default clip planes are inherited from the parent plot or scene. You can remove parent <code>clip_planes</code> by passing <code>Plane3f[]</code>.</p><p><strong><code>color</code></strong> = <code>nothing</code> — <em>No docs available.</em></p><p><strong><code>colormap</code></strong> = <code>@inherit colormap</code> — <em>No docs available.</em></p><p><strong><code>colorrange</code></strong> = <code>automatic</code> — The values representing the start and end points of <code>colormap</code>.</p><p><strong><code>colorscale</code></strong> = <code>identity</code> — The color transform function. Can be any function, but only works well together with <code>Colorbar</code> for <code>identity</code>, <code>log</code>, <code>log2</code>, <code>log10</code>, <code>sqrt</code>, <code>logit</code>, <code>Makie.pseudolog10</code>, <code>Makie.Symlog10</code>, <code>Makie.AsinhScale</code>, <code>Makie.SinhScale</code>, <code>Makie.LogScale</code>, <code>Makie.LuptonAsinhScale</code>, and <code>Makie.PowerScale</code>.</p><p><strong><code>depth_shift</code></strong> = <code>0.0</code> — Adjusts the depth value of a plot after all other transformations, i.e. in clip space, where <code>-1 &lt;= depth &lt;= 1</code>. This only applies to GLMakie and WGLMakie and can be used to adjust render order (like a tunable overdraw).</p><p><strong><code>fxaa</code></strong> = <code>false</code> — Adjusts whether the plot is rendered with fxaa (fast approximate anti-aliasing, GLMakie only). Note that some plots implement a better native anti-aliasing solution (scatter, text, lines). For them <code>fxaa = true</code> generally lowers quality. Plots that show smoothly interpolated data (e.g. image, surface) may also degrade in quality as <code>fxaa = true</code> can cause blurring.</p><p><strong><code>highclip</code></strong> = <code>automatic</code> — The color for any value above the colorrange.</p><p><strong><code>inspectable</code></strong> = <code>@inherit inspectable</code> — Sets whether this plot should be seen by <code>DataInspector</code>. The default depends on the theme of the parent scene.</p><p><strong><code>inspector_clear</code></strong> = <code>automatic</code> — Sets a callback function <code>(inspector, plot) -&gt; ...</code> for cleaning up custom indicators in DataInspector.</p><p><strong><code>inspector_hover</code></strong> = <code>automatic</code> — Sets a callback function <code>(inspector, plot, index) -&gt; ...</code> which replaces the default <code>show_data</code> methods.</p><p><strong><code>inspector_label</code></strong> = <code>automatic</code> — Sets a callback function <code>(plot, index, position) -&gt; string</code> which replaces the default label generated by DataInspector.</p><p><strong><code>joinstyle</code></strong> = <code>@inherit joinstyle</code> — Controls the rendering at corners. Options are <code>:miter</code> for sharp corners, <code>:bevel</code> for &quot;cut off&quot; corners, and <code>:round</code> for rounded corners. If the corner angle is below <code>miter_limit</code>, <code>:miter</code> is equivalent to <code>:bevel</code> to avoid long spikes.</p><p><strong><code>linecap</code></strong> = <code>@inherit linecap</code> — Sets the type of line cap used. Options are <code>:butt</code> (flat without extrusion), <code>:square</code> (flat with half a linewidth extrusion) or <code>:round</code>.</p><p><strong><code>linecolor</code></strong> = <code>@inherit linecolor</code> — <em>No docs available.</em></p><p><strong><code>linestyle</code></strong> = <code>nothing</code> — Sets the dash pattern of the line. Options are <code>:solid</code> (equivalent to <code>nothing</code>), <code>:dot</code>, <code>:dash</code>, <code>:dashdot</code> and <code>:dashdotdot</code>. These can also be given in a tuple with a gap style modifier, either <code>:normal</code>, <code>:dense</code> or <code>:loose</code>. For example, <code>(:dot, :loose)</code> or <code>(:dashdot, :dense)</code>.</p><p>For custom patterns have a look at <a href="./@ref"><code>Makie.Linestyle</code></a>.</p><p><strong><code>linewidth</code></strong> = <code>@inherit linewidth</code> — Sets the width of the line in screen units</p><p><strong><code>lowclip</code></strong> = <code>automatic</code> — The color for any value below the colorrange.</p><p><strong><code>miter_limit</code></strong> = <code>@inherit miter_limit</code> — Sets the minimum inner join angle below which miter joins truncate. See also <code>Makie.miter_distance_to_angle</code>.</p><p><strong><code>model</code></strong> = <code>automatic</code> — Sets a model matrix for the plot. This overrides adjustments made with <code>translate!</code>, <code>rotate!</code> and <code>scale!</code>.</p><p><strong><code>n_points</code></strong> = <code>automatic</code> — <em>No docs available.</em></p><p><strong><code>nan_color</code></strong> = <code>:transparent</code> — The color for NaN values.</p><p><strong><code>overdraw</code></strong> = <code>false</code> — Controls if the plot will draw over other plots. This specifically means ignoring depth checks in GL backends</p><p><strong><code>rasterize</code></strong> = <code>@inherit rasterize false</code> — Controls whether CairoMakie rasterizes a plot (true, &gt;0) or not (false, 0). If an integer is given, it sets the pixel density for rasterization.</p><p><strong><code>space</code></strong> = <code>:data</code> — Sets the transformation space for box encompassing the plot. See <code>Makie.spaces()</code> for possible inputs.</p><p><strong><code>ssao</code></strong> = <code>false</code> — Adjusts whether the plot is rendered with ssao (screen space ambient occlusion). Note that this only makes sense in 3D plots and is only applicable with <code>fxaa = true</code>.</p><p><strong><code>transformation</code></strong> = <code>:automatic</code> — Controls the inheritance or directly sets the transformations of a plot. Transformations include the transform function and model matrix as generated by <code>translate!(...)</code>, <code>scale!(...)</code> and <code>rotate!(...)</code>. They can be set directly by passing a <code>Transformation()</code> object or inherited from the parent plot or scene. Inheritance options include:</p><ul><li><p><code>:automatic</code>: Inherit transformations if the parent and child <code>space</code> is compatible</p></li><li><p><code>:inherit</code>: Inherit transformations</p></li><li><p><code>:inherit_model</code>: Inherit only model transformations</p></li><li><p><code>:inherit_transform_func</code>: Inherit only the transform function</p></li><li><p><code>:nothing</code>: Inherit neither, fully disconnecting the child&#39;s transformations from the parent</p></li></ul><p>Another option is to pass arguments to the <code>transform!()</code> function which then get applied to the plot. For example <code>transformation = (:xz, 1.0)</code> which rotates the <code>xy</code> plane to the <code>xz</code> plane and translates by <code>1.0</code>. For this inheritance defaults to <code>:automatic</code> but can also be set through e.g. <code>(:nothing, (:xz, 1.0))</code>.</p><p><strong><code>transparency</code></strong> = <code>false</code> — Adjusts how the plot deals with transparency. In GLMakie <code>transparency = true</code> results in using Order Independent Transparency.</p><p><strong><code>visible</code></strong> = <code>true</code> — Controls whether the plot gets rendered or not.</p>',49)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[114]||(e[114]=[o("a",{href:"https://github.com/MakieOrg/Makie.jl/blob/master/src/recipes.jl#L556-L685",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",B,[o("summary",null,[e[116]||(e[116]=o("a",{id:"TimeseriesMakie.trail!",href:"#TimeseriesMakie.trail!"},[o("span",{class:"jlbinding"},"TimeseriesMakie.trail!")],-1)),e[117]||(e[117]=t()),i(s,{type:"info",class:"jlObjectType jlFunction",text:"Function"})]),e[119]||(e[119]=o("p",null,[o("code",null,"trail!"),t(" is the mutating variant of plotting function "),o("code",null,"trail"),t(". Check the docstring for "),o("code",null,"trail"),t(" for further information.")],-1)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[118]||(e[118]=[o("a",{href:"https://github.com/MakieOrg/Makie.jl/blob/master/src/recipes.jl#L558",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",R,[o("summary",null,[e[120]||(e[120]=o("a",{id:"TimeseriesMakie.trajectory",href:"#TimeseriesMakie.trajectory"},[o("span",{class:"jlbinding"},"TimeseriesMakie.trajectory")],-1)),e[121]||(e[121]=t()),i(s,{type:"info",class:"jlObjectType jlFunction",text:"Function"})]),e[123]||(e[123]=c('<div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light-ianthina-comments github-dark-ianthina-comments vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">trajectory</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x, y, [z]; kwargs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Plots a colored trajectory in 2D or 3D space.</p><p><strong>Key attribtues:</strong></p><p><code>color</code> = <code>:speed</code>: The coloring method for the trajectory.</p><p>Can be one of:</p><ul><li><p><code>:speed</code>: Color by speed along the trajectory</p></li><li><p><code>:time</code>: Color by time index</p></li><li><p><code>&lt;: AbstractVector</code>: Color by a custom vector of values</p></li><li><p><code>Union{&lt;:Symbol, &lt;:Colorant}</code>: Color by a fixed color</p></li></ul><p><em>Other attributes are shared with <code>Makie.Lines</code>.</em></p><p><strong>Plot type</strong></p><p>The plot type alias for the <code>trajectory</code> function is <code>Trajectory</code>.</p><p><strong>Attributes</strong></p><p><strong><code>alpha</code></strong> = <code>1.0</code> — The alpha value of the colormap or color attribute. Multiple alphas like in <code>plot(alpha=0.2, color=(:red, 0.5))</code>, will get multiplied.</p><p><strong><code>clip_planes</code></strong> = <code>@inherit clip_planes automatic</code> — Clip planes offer a way to do clipping in 3D space. You can set a Vector of up to 8 <code>Plane3f</code> planes here, behind which plots will be clipped (i.e. become invisible). By default clip planes are inherited from the parent plot or scene. You can remove parent <code>clip_planes</code> by passing <code>Plane3f[]</code>.</p><p><strong><code>color</code></strong> = <code>:speed</code> — The coloring method for the trajectory</p><p><strong><code>colormap</code></strong> = <code>@inherit colormap :viridis</code> — Sets the colormap that is sampled for numeric <code>color</code>s. <code>PlotUtils.cgrad(...)</code>, <code>Makie.Reverse(any_colormap)</code> can be used as well, or any symbol from ColorBrewer or PlotUtils. To see all available color gradients, you can call <code>Makie.available_gradients()</code>.</p><p><strong><code>colorrange</code></strong> = <code>automatic</code> — The values representing the start and end points of <code>colormap</code>.</p><p><strong><code>colorscale</code></strong> = <code>identity</code> — The color transform function. Can be any function, but only works well together with <code>Colorbar</code> for <code>identity</code>, <code>log</code>, <code>log2</code>, <code>log10</code>, <code>sqrt</code>, <code>logit</code>, <code>Makie.pseudolog10</code>, <code>Makie.Symlog10</code>, <code>Makie.AsinhScale</code>, <code>Makie.SinhScale</code>, <code>Makie.LogScale</code>, <code>Makie.LuptonAsinhScale</code>, and <code>Makie.PowerScale</code>.</p><p><strong><code>cycle</code></strong> = <code>[:color]</code> — Sets which attributes to cycle when creating multiple plots. The values to cycle through are defined by the parent Theme. Multiple cycled attributes can be set by passing a vector. Elements can</p><ul><li><p>directly refer to a cycled attribute, e.g. <code>:color</code></p></li><li><p>map a cycled attribute to a palette attribute, e.g. <code>:linecolor =&gt; :color</code></p></li><li><p>map multiple cycled attributes to a palette attribute, e.g. <code>[:linecolor, :markercolor] =&gt; :color</code></p></li></ul><p><strong><code>depth_shift</code></strong> = <code>0.0</code> — Adjusts the depth value of a plot after all other transformations, i.e. in clip space, where <code>-1 &lt;= depth &lt;= 1</code>. This only applies to GLMakie and WGLMakie and can be used to adjust render order (like a tunable overdraw).</p><p><strong><code>fxaa</code></strong> = <code>false</code> — Adjusts whether the plot is rendered with fxaa (fast approximate anti-aliasing, GLMakie only). Note that some plots implement a better native anti-aliasing solution (scatter, text, lines). For them <code>fxaa = true</code> generally lowers quality. Plots that show smoothly interpolated data (e.g. image, surface) may also degrade in quality as <code>fxaa = true</code> can cause blurring.</p><p><strong><code>highclip</code></strong> = <code>automatic</code> — The color for any value above the colorrange.</p><p><strong><code>inspectable</code></strong> = <code>@inherit inspectable</code> — Sets whether this plot should be seen by <code>DataInspector</code>. The default depends on the theme of the parent scene.</p><p><strong><code>inspector_clear</code></strong> = <code>automatic</code> — Sets a callback function <code>(inspector, plot) -&gt; ...</code> for cleaning up custom indicators in DataInspector.</p><p><strong><code>inspector_hover</code></strong> = <code>automatic</code> — Sets a callback function <code>(inspector, plot, index) -&gt; ...</code> which replaces the default <code>show_data</code> methods.</p><p><strong><code>inspector_label</code></strong> = <code>automatic</code> — Sets a callback function <code>(plot, index, position) -&gt; string</code> which replaces the default label generated by DataInspector.</p><p><strong><code>joinstyle</code></strong> = <code>@inherit joinstyle</code> — Controls the rendering at corners. Options are <code>:miter</code> for sharp corners, <code>:bevel</code> for &quot;cut off&quot; corners, and <code>:round</code> for rounded corners. If the corner angle is below <code>miter_limit</code>, <code>:miter</code> is equivalent to <code>:bevel</code> to avoid long spikes.</p><p><strong><code>linecap</code></strong> = <code>@inherit linecap</code> — Sets the type of line cap used. Options are <code>:butt</code> (flat without extrusion), <code>:square</code> (flat with half a linewidth extrusion) or <code>:round</code>.</p><p><strong><code>linestyle</code></strong> = <code>nothing</code> — Sets the dash pattern of the line. Options are <code>:solid</code> (equivalent to <code>nothing</code>), <code>:dot</code>, <code>:dash</code>, <code>:dashdot</code> and <code>:dashdotdot</code>. These can also be given in a tuple with a gap style modifier, either <code>:normal</code>, <code>:dense</code> or <code>:loose</code>. For example, <code>(:dot, :loose)</code> or <code>(:dashdot, :dense)</code>.</p><p>For custom patterns have a look at <a href="./@ref"><code>Makie.Linestyle</code></a>.</p><p><strong><code>linewidth</code></strong> = <code>@inherit linewidth</code> — Sets the width of the line in screen units</p><p><strong><code>lowclip</code></strong> = <code>automatic</code> — The color for any value below the colorrange.</p><p><strong><code>miter_limit</code></strong> = <code>@inherit miter_limit</code> — Sets the minimum inner join angle below which miter joins truncate. See also <code>Makie.miter_distance_to_angle</code>.</p><p><strong><code>model</code></strong> = <code>automatic</code> — Sets a model matrix for the plot. This overrides adjustments made with <code>translate!</code>, <code>rotate!</code> and <code>scale!</code>.</p><p><strong><code>nan_color</code></strong> = <code>:transparent</code> — The color for NaN values.</p><p><strong><code>overdraw</code></strong> = <code>false</code> — Controls if the plot will draw over other plots. This specifically means ignoring depth checks in GL backends</p><p><strong><code>rasterize</code></strong> = <code>@inherit rasterize false</code> — Controls whether CairoMakie rasterizes a plot (true, &gt;0) or not (false, 0). If an integer is given, it sets the pixel density for rasterization.</p><p><strong><code>space</code></strong> = <code>:data</code> — Sets the transformation space for box encompassing the plot. See <code>Makie.spaces()</code> for possible inputs.</p><p><strong><code>ssao</code></strong> = <code>false</code> — Adjusts whether the plot is rendered with ssao (screen space ambient occlusion). Note that this only makes sense in 3D plots and is only applicable with <code>fxaa = true</code>.</p><p><strong><code>transformation</code></strong> = <code>:automatic</code> — Controls the inheritance or directly sets the transformations of a plot. Transformations include the transform function and model matrix as generated by <code>translate!(...)</code>, <code>scale!(...)</code> and <code>rotate!(...)</code>. They can be set directly by passing a <code>Transformation()</code> object or inherited from the parent plot or scene. Inheritance options include:</p><ul><li><p><code>:automatic</code>: Inherit transformations if the parent and child <code>space</code> is compatible</p></li><li><p><code>:inherit</code>: Inherit transformations</p></li><li><p><code>:inherit_model</code>: Inherit only model transformations</p></li><li><p><code>:inherit_transform_func</code>: Inherit only the transform function</p></li><li><p><code>:nothing</code>: Inherit neither, fully disconnecting the child&#39;s transformations from the parent</p></li></ul><p>Another option is to pass arguments to the <code>transform!()</code> function which then get applied to the plot. For example <code>transformation = (:xz, 1.0)</code> which rotates the <code>xy</code> plane to the <code>xz</code> plane and translates by <code>1.0</code>. For this inheritance defaults to <code>:automatic</code> but can also be set through e.g. <code>(:nothing, (:xz, 1.0))</code>.</p><p><strong><code>transparency</code></strong> = <code>false</code> — Adjusts how the plot deals with transparency. In GLMakie <code>transparency = true</code> results in using Order Independent Transparency.</p><p><strong><code>visible</code></strong> = <code>true</code> — Controls whether the plot gets rendered or not.</p>',43)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[122]||(e[122]=[o("a",{href:"https://github.com/MakieOrg/Makie.jl/blob/master/src/recipes.jl#L556-L680",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",G,[o("summary",null,[e[124]||(e[124]=o("a",{id:"TimeseriesMakie.trajectory!",href:"#TimeseriesMakie.trajectory!"},[o("span",{class:"jlbinding"},"TimeseriesMakie.trajectory!")],-1)),e[125]||(e[125]=t()),i(s,{type:"info",class:"jlObjectType jlFunction",text:"Function"})]),e[127]||(e[127]=o("p",null,[o("code",null,"trajectory!"),t(" is the mutating variant of plotting function "),o("code",null,"trajectory"),t(". Check the docstring for "),o("code",null,"trajectory"),t(" for further information.")],-1)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[126]||(e[126]=[o("a",{href:"https://github.com/MakieOrg/Makie.jl/blob/master/src/recipes.jl#L558",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),o("details",Y,[o("summary",null,[e[128]||(e[128]=o("a",{id:"TimeseriesMakie.widthprofile-NTuple{6, Any}",href:"#TimeseriesMakie.widthprofile-NTuple{6, Any}"},[o("span",{class:"jlbinding"},"TimeseriesMakie.widthprofile")],-1)),e[129]||(e[129]=t()),i(s,{type:"info",class:"jlObjectType jlMethod",text:"Method"})]),e[131]||(e[131]=c('<div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light-ianthina-comments github-dark-ianthina-comments vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">widthprofile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(mode, xy, P, widthrange, crowding, taper)</span></span></code></pre></div><p>One width per point, from <code>mode</code>: a <code>Number</code> for a constant width, <code>:x</code> or <code>:y</code> for that coordinate mapped onto <code>widthrange</code>, or <code>:curv</code> for the room the curve has at each point.</p><p><code>xy</code> are the data-space points and <code>P</code> the same points in pixels; only <code>:curv</code> needs <code>P</code>.</p>',3)),i(s,{type:"info",class:"source-link",text:"source"},{default:a(()=>[...e[130]||(e[130]=[o("a",{href:"https://github.com/brendanjohnharris/TimeseriesMakie.jl/blob/main/src/recipes/kinetic.jl#L238-L245",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})])])}const Q=r(p,[["render",U]]);export{J as __pageData,Q as default};
